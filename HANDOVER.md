# 引き継ぎ書

最終更新: 2026-08-06 / コミット `fa00bdc`

---

## 1. これは何か

Terraforming Mars の非公式Web実装。公式ルール準拠を目指している。

**公開先は2つ。同じソースから別ビルドを出している。**

| 公開先 | URL | 内容 |
|---|---|---|
| GitHub Pages | https://akatsuki11horizon26-create.github.io/terraforming-mars-online/ | ソロ・ロボット戦専用（サーバーなし） |
| Cloudflare Workers | https://mars-frontier.gameofai.workers.dev | 上記＋オンライン対戦 |

ソロ版は `NEXT_PUBLIC_SOLO_ONLY=1` でビルドされ、`ONLINE_ENABLED` が false になる。
**サーバーが存在しないので、ソロ版のUIはエンジンを直接呼ぶ。** これは設計上の意図であり、
「UIはルールを持たない」原則とは両立する（呼び出すだけで、ルールは持たない）。

### 技術構成

- Next.js 16.2.6 / React 19.2.6 / vinext / Vite 8 / TypeScript 5.9.3
- Cloudflare Workers + Durable Objects（1部屋1DO）、WebSocket
- テスト: `node --test`、312件
- CI: GitHub Actions（lint / 型 / テスト / 全拡張プレイテスト / 5マップ）

---

## 2. 最重要: 進行中の改修

**「アクション実行経路の一本化」を実施中。全8フェーズ中、8つとも完了。**
残っているのは移行そのものではなく、下記「残っている直接変更」の後始末。

### 背景

同じ「カードをプレイする」処理が4箇所に別実装で存在していた。
そのため片方だけ直したバグが何度も再発した（配置ボーナス二重加算、TR加算漏れ、
ロボットの海洋二重加算など、実際に4件踏んでいる）。

### 現在の構造

```
オンライン: UI ──command──> Durable Object ──> executeGameCommand
ソロ:      UI ────────────command────────────> executeGameCommand
Bot:       ────────────────command────────────> executeGameCommand
```

**`app/game-command.js` が唯一の状態変更入口。** ここが引き受けているもの:

- 手番検証（コマンドが名乗る席からのものか）
- 所持検証（手札にあるか、場に出しているか）
- **アクション消費**（成功時のみ1回。選択を伴う場合は最後の選択時に1回）
- 企業トリガーとパラメータ閾値ボーナス

### 完了済み

| # | 項目 |
|---|---|
| 1 | 選択完了時のアクション消費 |
| 2 | カードプレイ後の共通処理（企業トリガー・閾値ボーナス） |
| 3 | 研究購入のコマンド化（`BUY_RESEARCH`） |
| 4 | オンライン盤面をサーバーstateで描画 |
| 5 | 標準プロジェクト8種のコマンド化 |
| 6 | オンライン支払い情報の送信（建材・チタン、不正値クランプ） |
| 8 | CI + 統合テスト |
| 7 | 企業アクションのコマンド化（`CORPORATION_ACTION`） |

### 項目7でやったこと

`COMMAND.CORPORATION_ACTION` を新設し、Ecoline / UNMI / Robinson の3社を
`app/game-command.js` の `CORPORATION_ACTIONS` に移した。
`CORPORATION_ACTION_ID` もエンジン側の定義に一本化してある（page.tsx はimportするだけ）。

- Ecolineは `placeOrAsk` を共用する。植物7はアクション開始時に払い、
  配置先を聞いている間もアクションは消費しない。答えた時点で1回だけ消費する。
  緑地は酸素を上げるので `checkParameterThresholds` を通している
- UNMIの「この世代にTRが上がっていれば」は `generationStartTr` で判定する。
  **これはプレイヤー単位のフィールドなので、必ず `getPlayer` 経由で読むこと**
- 使用済みフラグは実行前に立てる。配置を聞いている最中に二度目を開始できないため

オンラインは `worker/room.ts` の対応表に `corporationAction` を足しただけ。

### 外部監査（`be57356` 時点）への対応

**監査は `be57356` 時点のスナップショットに対するもので、判定は現HEADで再測定した。**
指摘のうち1件は現HEADでは誤り、1件は文言が不正確だったが中身は本物、残りは再現した。

| 指摘 | 現HEADでの実測 | 対応 |
|---|---|---|
| ソロUIが直接 `resolvePendingChoice` | **本物**。都市25MC払ってアクション消費0 | `c054678` |
| ボットも直接呼んでいる | **誤り**。`resolveBotChoices` は `applyBotMove` の後始末で、呼び出し元が既に `actionSpent: true` を返している。`RESOLVE_PENDING` に置換すると**二重消費**になる | 変更せず |
| 選択カードでトリガー未適用 | **本物**。Saturn Systems + Ganymede Colony で生産量が上がらない | `9a20bb8` |
| 標準プロジェクトの不足処理 | **全項目本物**。特に配置不能時に25MC支払い済みstateを返す／特許売却の重複IDで1枚が2MC | `9a20bb8` |
| 「緑地で酸素8%超えたら気温上昇」 | 文言は不正確（ルールは酸素8%達成で気温+1段階、実装済み）だが、`greenery` が `finishProject` に `before` を渡しておらず**閾値が走らないのは本物** | `9a20bb8` |
| オンラインUIが `gameState` 参照 | **本物**。ローカルの別ゲームを表示していた | `19f327d` |

**測り方**: 一時スクリプトで状態の前後差分を取った。「コードを読んで判断」ではなく実測。
`ok=false` でも `result.state` が汚れている、という種類のバグはコード読解では見落とす。

### この対応で踏んだ罠（再発しやすい）

1. **`placeTileAt` に企業の配置効果を足すときは `applyCorporationInitialAction` を確認する。**
   `placeTile` は `placeTileAt` に委譲しているので、両方に書くと初期都市だけ二重になる。
   指摘を鵜呑みに `placeTileAt` へ足していたら新しいバグを作っていた
2. **企業の乱数を読むテストを自分でも書いた。** 「都市でmcProd +1」は Tharsis を引くと +2。
   13社中2社で落ちるので **約15%** の確率で落ちるフレーキーになる。
   既存テストの「都市は25MC」も同じ理由で壊れた（CrediCor還付・Tharsis報酬）。
   **企業効果に依存しない値を測りたいなら `corporationId: null` を明示すること**
3. **`gameState` → `activeState` の機械的置換は2箇所で誤爆した。**
   オンボーディングの既読フラグと、新規ゲーム設定パネルの既定値は
   **ローカルに書いてローカルから読む**ので `gameState` が正しい。
   書き込み先と読み取り元が一致しているかで判断する

### 外部監査（2回目・`0152e25` 時点）への対応

**監査は「前回から変更なし」を前提にしていたが、これは正しかった。**
`c054678`〜`3e79b84` の9コミットが**ローカルに溜まったままpushされていなかった**ため、
リモートは実際に `0152e25` のままだった。監査の前提を疑う前に `git log origin/main` を見ること。

| 指摘 | 対応 |
|---|---|
| 選択処理の共通コマンド化 | `c054678` で対応済みだった |
| 選択完了後の後処理（トリガー・閾値・完了ログ） | `afterPlay` continuation を新設。`f8e8aa7` |
| Cloudflare版UIの `activeState` 化 | `19f327d` で対応済みだった |
| 企業アクション・最終緑化のコマンド化 | `COMMAND.CORPORATION_ACTION` を新設。`f8e8aa7` |
| 標準プロジェクトのルール修正 | `f8e8aa7`。ただしキー名の指摘2件は**採用しなかった**（下記） |
| 特許売却UIが到達不能 | `25c4283` |
| Beginner Corporation の初期カード無料 | `f8e8aa7` |
| フェーズと pending 中の操作検証 | `e46d103` + `f60babe` |

**採用しなかった2件**（どちらも「キー名の綴り違い」に見えるが、実際は設計判断）:

- `powerPlantDiscount` → `powerDiscount`: Thorgate の効果文は
  **Powerタグを持つカード**への割引であって、発電所標準プロジェクトではない。定数11に固定した
- `greeneryPlantCost` → `plantGreeneryCost`: このコードベースは Ecoline の植物7個を
  **世代1回の企業アクション**として与えている。ここでも読むと二重取りになる。定数8に固定した

どちらもコード中にコメントで理由を残してある。

### 残っている直接変更

```
.mc  ±=   0箇所
.tr  ±=   1箇所   （最終緑化でTRを戻す1行）
Prod ±=   0箇所
```

`e0167c7` の到達不能コード削除で `.mc` と生産量の直接変更は**UIから消えた**。
残る `.tr` 1箇所は最終緑化フェーズで `placeTile` が上げたTRを戻すもので、
engine が「最終緑化ではTRも酸素も動かない」を知らないための後始末。
engine 側に寄せるのが本筋だが未実施。

**`app/page.tsx` の到達不能な配置分岐は削除済み（`e0167c7`、164行減）。**

`placementMode` の生きた入口は **`handleFinalGreeneryConvert` 1箇所だけ**で、
`type: "forest"` しか入らない。カードのプレイも標準プロジェクトもコマンド層を通るので、
`handleCellClick` に残っていたカード支払い・標準プロジェクト3分岐・複数配置カウンタは
すべて誰も通らないコードだった。

**この削除で一度誤判断しかけた。** 引き継ぎ書の旧版は下半分の
Mining Guild / Tharsis Republic ブロックを「カード配置からも通る生きた経路」と書いていたが、
これは `grantPlacementCorporationEffects` を engine 側に入れる**前**の記述で、既に古かった。

実測すると `placeTileAt` だけで Tharsis は **mcProd +1 / MC +3** を既に払っており、
UI側のブロックが動けば**二重取り**になる。ただし到達性を調べると
`placementMode.type` は `"forest"` 固定なので `type === "city"` が真になる経路が無く、
**実害は出ていなかった**。

教訓: **「二重に見える」と「二重に動く」は別。**
実測（効果が出るか）と到達性（そこへ来るか）は**両方**確かめること。
片方だけで消すと、生きたコードを削るか、死んだコードを残すかのどちらかになる。

---

## 3. このコードベースで繰り返し踏んだ罠

### 3.1 非列挙アクセサ（最重要）

`state.mc` は `players[currentPlayerId].mc` を指すアクセサ。**`Object.defineProperty` で
`enumerable: false` として張られている。**

```js
const next = { ...state };   // ← アクセサが消える。事故の最大の原因
```

正しくは `cloneGameState(state)` を使う。`viewForPlayer` でもこれを踏んで、
オンラインの全クライアントが「手番の人の手札」を読む状態になっていた。

**将来的には廃止して `players[id]` 明示に移行すべき**（sol の助言）。
ただし移行するなら、ロード境界で一度だけ変換し、
`state.mc ?? players[id].mc` のような恒久フォールバックは作らないこと。

### 3.2 テストが乱数を読んでいる

**5回以上これで痛い目を見ている。** 症状は「3〜4回に1回落ちる」。

原因のパターン:

- **企業がランダム** — 6社が初期建材生産1、4社が初期電力生産1、
  Ecolineは初期植物3を持つ。「生産量が1になる」と書くと落ちる
- **植民地タイルがランダム** — タイルごとに交易報酬の通貨が違う。
  Luna は M€ を払うので、支払い9MCと相殺されて総額が変わる
- **ヘルパーが毎回別のゲームを配る** — `seed()` を2回呼んで比較すると、
  違うタイル・違う企業を比べることになる

**対策: 絶対値ではなく差分で測る。1つのゲームを作って `cloneGameState` で分岐させる。**

```js
// 悪い例
assert.equal(after.energyProd, 1);

// 良い例
assert.equal(after.energyProd, before.energyProd + 1);
```

**差分でも足りない場合は企業を固定する。** 「カード3枚で9MC」のように
*企業効果そのものが値を変える*ケースは、差分を取っても Beginner を引けば0MCになる。
このセッションで3回踏んだ。3回目は **CIでしか落ちなかった**（13社中1社なので約8%、
ローカルの6連続成功はただの運）。

```js
// 企業効果を排除して標準価格を測りたいとき
state = cloneGameState(state);
state.players = state.players.map(p =>
  p.id === buyer ? { ...p, corporationId: "corp-teractor" } : p
);
```

**ローカルで数回通っても「直った」とは言えない。落ちる確率を見積もってから回数を決めること。**

**「1ゲームをforkすれば固定される」は嘘のことがある。** 交易テストは
`getInitialState` を毎回呼ぶので、fork してもタイル自体が毎回違う。
Callisto は交易報酬に電力2を払うため `5 - 3 + 2 = 4` となり、
「支払い後は2」と書いた検証が**約6回に1回**落ちていた（`3e83f27` で修正）。

**支払い後の残高は価格ではない。** 収入を伴う操作では、
初期値だけが違う2本を比べて差を取れば収入が相殺されて価格だけが残る。

### 3.2.1 外部監査（3回目）で見つかった既存バグ

**特殊VP3枚（Law Suit / Vermin / St. Joseph）の実装依頼から入ったが、
その前提として直す必要のある既存バグが7件出てきた。** すべて実測で確認済み。

| # | 症状 | 実測 | 修正 |
|---|---|---|---|
| 1 | **攻撃カードが自分を攻撃する** | Asteroidをプレイすると**自分の植物が3個減り、相手は無傷**。10枚が該当 | `cf376d5` |
| 2 | 生産量攻撃が消える | 合法対象が1人だと直接適用も選択生成もされず**攻撃が丸ごと消滅** | `cf376d5` |
| 3 | イベントのタグが永続 | 79枚のイベントが `playedProjects` に残り、Earth/Space/Science等のタグを**ゲーム終了まで数え続ける** | `a3822c0` |
| 4 | Preludeのタグが数えられない | Jovian Prelude 3枚でGanymede Colonyが**+0点**（正しくは+3） | `a3822c0` |
| 5 | Vitorが動的VPに反応しない | `victoryPoints` だけ見ており、`victoryPointSpec` の**34枚でMC3が出ない** | `614ab38` |
| 6 | Botが選択後にアクションを消費しない | Asteroid使用時 **2→2**（消費されない）。閾値ボーナスも入らない | `614ab38` |
| 7 | 最終得点の内訳が合計と一致しない | 内訳は固定VPのみ。動的VP・Prelude・称号・褒賞が**すべて0表示** | `614ab38` |

**1と2は「Law Suitの前提」として指摘されたが、単独で最も重いバグだった。**
攻撃カードは対象を選ぶ `pendingChoice` を出すようになり、
実際に減った場合だけ `generationAttackLedger` に記録する。

**踏んだ罠**: イベントを `playedProjects` から出すと **Legendマイルストーン**
（イベント5枚以上）が壊れる。`cardTypeCount` は両方の山を読むようにした。
旧セーブはイベントが `playedProjects` に残ったままなので、これがないと過去のセーブで0枚になる。

**8件目: 選択式カードアクションがアクションを消費していなかった**（`4df3866`）。
`applyCardAction` の分岐選択が `consumedAction: false` を渡していたため、
Verminは毎ターン無償で動物を増やせた。分岐の選択は「アクションの一部」なので `true` が正しい。
使用済みフラグは分岐確定より後に立つので、二重消費にはならない（確認済み）。

### 3.3 `npm test` はビルドを挟む

`npm test` は `npm run build` を実行してからテストする。
ビルド中に走ると古い成果物を読んで落ちることがある。

**1回の失敗では判断しない。3回連続で確認すること。**

また `static-dist/index.html` を読むテストが2件ある。
ローカルには過去のビルドが残っていて通るが、CIには無い。
CIには `npm run build:static` を入れてある。

### 3.4 「成功を返すが何もしない」

エラーを出さず条件が偽になるだけのコードが複数あった。

- `applyCardAction` が `cardId` を渡さず、資源が置かれない
- `getCardPlayableStatus` は「コストと条件」の判定で、**所持は見ない**。
  手札0枚でも428枚中272枚が `playable: true` を返す

**アクションが成功を返しても、状態が変わったか実測すること。**

**得点計算も同じ性質を持つ。** `computeScore` は該当カードを見つけられなければ
黙って加算しないだけで、例外もログも出ない。実際に4枚が0点になっていた
（`3e83f27` `f743c2f`）。**カードを1枚ずつ「置いた場合と置かない場合の差」で
測らないと発見できない。** 合計点だけ見ても妥当な値に見えてしまう。

---

## 4. 主要ファイル

| ファイル | 役割 |
|---|---|
| `app/game-command.js` | **唯一の状態変更入口**。ここにルールを追加する |
| `app/game-logic.js` | エンジン本体（約3000行）。カード効果・盤面・フェーズ |
| `app/page.tsx` | UI。ルールを持たない（移行途中） |
| `worker/room.ts` | Durable Object。認証・配信・永続化。ルールを持たない |
| `app/bot-player.js` | ボットAI。合法手を選ぶだけ |
| `app/player-state.js` | アクセサ互換層（3.1参照） |
| `app/net-protocol.js` | 通信形式と `viewForPlayer`（プライバシーフィルタ） |
| `app/draft.js` | ドラフト制 |
| `app/alternate-boards.js` | 4マップの盤面データ（生成物） |
| `app/board-milestones.js` | マップ別の称号・褒賞 |
| `app/card-art.data.js` | 428枚のカードアート（生成物、689KB） |

### 生成スクリプト

```
scripts/generate-boards.mjs        4マップ盤面（リファレンス実装から移植）
scripts/generate-japanese-text.mjs カード名・効果の日本語
scripts/build-card-art.mjs         SVGアートの検証とバンドル
scripts/build-art-briefs.mjs       アート生成用の指示文
scripts/check-card-art.mjs         主題が中央にあるかの機械検査
scripts/playtest.mjs               完全なゲームを回して不変条件を検査
```

---

## 5. 実装済みの主な機能

- **カード428枚**（基本・Colonies・Prelude・Prelude2・Promo・Turmoil・Venus）
  - 全カード日本語名。Venus は「金星」表記で統一
  - 未実装効果 **0枚**（テストで検証。1枚でもあれば落ちる）
- **カードアート428枚**（SVG、Codex生成）
- **5マップ**（タルシス・ヘラス・エリシウム・ユートピア平原・アマゾニス平原）
  - 各マップ固有の称号5種・褒賞5種
  - ヘラス南極（6MC払って海洋タイル）
- **ドラフト制**（ロビーで選択。初期10枚も対象。世代ごとに向きが反転）
- **ロボット戦**（3難易度。合法手を列挙して評価）
- **オンライン対戦**（部屋コード、5拡張＋5マップ選択）

---

## 6. 未確認・既知の課題

### 6.1 実機確認をしていない

**ブラウザで一度も動作確認していない。**
テスト312件・CI・プレイテストは通っているが、実際の操作感は未確認。

特に未確認なのは:
- 今回コマンド層に載せ替えた標準プロジェクト・カードプレイの操作感
- **企業アクションのUI**（Ecolineの配置先選択が `pendingChoice` 経由に変わった。
  以前は `placementMode` でUI側が配置を持っていたので、見え方が変わる可能性がある）
- **オンラインの表示全般（`19f327d`）**。`activeState` 基準へ移したが、
  **これはテストで担保できていない**。テスト312件もプレイテストもオフライン経路しか通らない。
  2端末繋いで、企業選択・Prelude・研究購入の各画面がサーバーの候補を出しているか確認すること
- 最終得点画面の内訳が緑地・都市を `placedBy === "player"` 固定で数えていた。
  合計点は `computeScore(state, playerId)` から来るので正しく、**内訳の行だけ**が
  ホットシート2人目以降で0になる表示バグだった。`currentPlayerId` に修正済み
- オンライン対戦の通しプレイ（2端末必要）
- **今回のUI変更4件（`09a5931` / `35844ed`）はすべてブラウザ未確認。**
  型・lint・ビルド・テスト312件は通っているが、これらは見た目を検証しない。
  特に確認が要るもの:
  - タイトル →「ソロプレイ」→ 拡張パネル → 開始、の導線が繋がっているか
  - タイル配置時、盤面の合法マスが光り、クリックで置けるか（バナーが操作を遮らないか）
  - カットインが画面中央に出て1秒で消えるか。盤面のクリックを妨げないか
  - 購入フェーズで手札が出るか。そこでカードを選んでもプレイできないか
    （エンジン側は `WRONG_PHASE` で拒否することを実測済み）
- **`f8e8aa7` / `25c4283` / `f60babe` のUI影響もブラウザ未確認。**
  - 特許売却モードに入れるか（`25c4283` で到達可能にしたが、押した先は未確認）
  - 企業アクション（Ecoline / UNMI / Robinson）のボタンが出て、押すと `CORPORATION_ACTION` が飛ぶか
  - pendingChoice が開いている間、他の操作が「先に選択を解決してください」で
    弾かれる（`f60babe`）のが**操作不能に見えないか**。エンジンは正しく拒否するが、
    UIが理由を表示しなければ「固まった」と受け取られる
- カットイン演出やアニメーション

### 6.2 ユーザー要望（全項目を実測して対応済み・1件を除く）

**このリストは古い状態のまま放置されていた。実測したところ7件中3件は既に実装済みだった。**

| # | 要望 | 実測結果 | 対応 |
|---|---|---|---|
| 1 | アクションカードに「トークンN個で勝利点」の記載がない | **本物**。38枚中17枚がカード面に `?` と表示されていた。加えて得点計算バグ4枚を発見（下記） | `3e83f27` `f743c2f` `9a79be1` |
| 2 | アクションカードは種類ごとに各1回 | **既に実装済み**。`usedCardActions` は `card.id` 単位。1枚使っても他は使える | 不要 |
| 3 | 購入フェーズで手札とシンボル集計を見たい | **本物**。手札は `phase === "action"` のときしか描画されず、購入中は見えなかった。シンボル集計はHUDから常時開けた | `35844ed` |
| 4 | 開始時に拡張選択画面 | **本物**。タイトルの「ソロ」が拡張オフ固定で即開始していた | `09a5931` |
| 5 | 金星オフ時に金星カードを混ぜない | **既に実装済み**。`poolFor`/`enabledExpansions` で除外済み（実測: オフ0枚 / オン45枚） | 不要 |
| 6 | パラメータ上昇のカットイン | 演出自体は存在したが右下に小さく1.8秒。要望は「中央に1秒」 | `35844ed` |
| 7 | 配置時に配置先が見えない | **本物**。選択ダイアログが不透明度82%＋ブラーで盤面を覆い、「(3, -2)」という座標のボタンから選ばせていた | `35844ed` |

#### 項目1: 動的VPカードの調査結果

ユーザーから対象42枚のリストを受領して調査した。**探すべきフィールドは
`victoryPoints` ではなく `victoryPointSpec`** で、カタログ側には38枚分が既に入っていた。
残る4枚（Law Suit / Vermin / St. Joseph / Search for Life）は専用処理扱い。

`computeScore` を実測したところ、**3件が黙って0点になっていた**。

| カード | 症状 | 原因 | 修正 |
|---|---|---|---|
| Nobel Prize | 固定2VPが入らない | `computeScore` が `playedProjects` しか走査せず、Preludeは `selectedPreludeIds` に居る | `3e83f27` |
| Main Belt Asteroids | 小惑星2個ごと1VPが入らない | 同上 | `3e83f27` |
| Immigration Shuttles | 都市3枚ごと1VPが**盤面が何枚でも0** | `all: true` の分岐が `cardPlacements` 参照の内側にあり、配置されないカードは到達しない | `f743c2f` |
| Space Port Colony | コロニー2個ごと1VPが同様に0 | 同上 | `f743c2f` |

**エラーは一切出ない。** 得点計算はカードを見つけられなければ黙って加算しないだけなので、
「テストが通っている」も「例外が出ない」も**この種のバグの証拠にならない**。

正しく動いていたもの（実測確認済み）: 比例VP（Birds / Ants / Tardigrades / Physics Complex
など資源系32枚）、木星タグ系3枚、Search for Life（科学資源1個以上で3VP）、
Capital（隣接海洋1枚1VP）、Cloud Tourism（`resourcesHere: 1` という他と違う書式だが
スコアラは `!== undefined` で見ているので通る）。

**Capital は二重計上しかけていた。** `computeScore` には汎用の `spec.oceans` 分岐と
`cardId === "p-capital"` のハードコードの**両方**がある。実測すると隣接海洋2枚で +2、
つまり二重にはなっていない——生きているカードIDが `p-capital` で、そちらの
`victoryPointSpec` が `null` だから汎用分岐に入らないという**偶然**による。
`card-base-capital` は配られていない重複エントリ。触るときは注意。

**表示側も直した（`9a79be1`）。** これが元の要望そのものだった。

`app/project-card.tsx` の `victoryPointLabel` は `per` も `each` も無い spec で
`"?"` を返していた。**「1個につき1VP」はまさにその書式**なので、
Birds / Capital / 木星タグ系など **38枚中17枚がカード面に「?」と出ていた**。

バッジは数字1つ分の幅しかないので、**何を数えるのかは `title` / `aria-label` に置いた**
（「資源2個ごとに1勝利点」など）。「1/2」だけでは *何の* 2個か分からない。

#### 特殊VP3枚の実装状況

**得点計算は完了（`b6fc34a`）。カード効果の実装は未着手。**

スコアラーを `app/scoring.js` に切り出し、**ゲーム全体から `ScoreContribution[]` を
生成してからプレイヤーごとに合算する**二段構成にした。
これがないと「他人に点を与えるカード」が原理的に書けない。

- `buildScoreContributions(state, options)` — 全寄与を一度に生成
- `calculateScoreBreakdowns(state)` — プレイヤーIDごとの内訳
- `computeScore(state, playerId)` — 既存呼び出し互換のラッパー

**合計は0で丸めない。** 負の得点はそのまま返る。

| カード | 得点 | 効果（プレイ処理） |
|---|---|---|
| Vermin | ✅ 終了時に全員へ都市数分の−VP | ✅ `placeTileAt` フックで全都市から動物+1。アクションも修正 |
| St. Joseph | ✅ `boardMarkers` の大聖堂数だけ所有者へ+VP | ✅ MC5で大聖堂設置。1都市1個、相手都市も可 |
| Law Suit | ✅ `scoreModifiers` から対象へ−1VP | ✅ 候補0/1/複数、最大MC3、対象の場に配置 |

すべて `4df3866` で実装。カード面のVP表示は `dad7643`。

#### 攻撃カードのモデル化（Law Suitの前提）

**「6枚」ではなく7枚だった。** 機械的に抽出したところ:

| カード | 資源 | 奪う/除去 | 備考 |
|---|---|---|---|
| Hired Raiders | 建材2 / MC3 | 奪う | |
| Sabotage | チタン3 / 建材4 / MC7 | 奪う | |
| Virus | 植物5 | 除去 | **動物2の分岐は未実装**（下記） |
| Air Raid | MC5 | 奪う | |
| Special Permit | 植物4 | 奪う | Turmoilのグリーン与党が前提 |
| Comet for Venus | MC4 | 奪う | **金星タグ所持者のみ**が対象 |
| Flooding | MC4 | 除去 | **任意**。海洋隣接タイルの所有者のみ |

**Crash Site Cleanup は攻撃ではない。** 「今世代に他プレイヤーの植物が除去されている」を
*条件として読む*カードで、ledgerの正しさを裏づける存在。

**Floodingにはもう1つバグがあった。** 海洋タイルが `{tr:{oceans:1}}` という
効果リーダーが解釈できない形で書かれており、**海洋を1枚も置いていなかった**。

**Virusの動物2個分岐は未実装。** カタログにカードの資源種別メタデータが無く、
「任意のカードから動物2個」を機械的に対象指定できない。植物5の分岐のみ動く。

#### 項目3の対応で見つかった副次バグ

手札を購入フェーズで表示した結果、**研究フェーズ中にカードがプレイできる**ことが判明した。
`ERROR.WRONG_PHASE` は定義されているだけで一度も使われておらず、
**フェーズ検証が丸ごと欠けていた**。UIが手札を隠していたことが唯一の防御だった。
`e46d103` でコマンド層に `checkPhase` を追加。

実測: 修正前は research フェーズでカードプレイが `ok=true` でMC10を消費していた。

### 6.3 外部監査で指摘され未検証の項目

61件の監査を受け、うち実測で確認できたものは修正済み。
**未検証のまま残っている主なもの**:

- World Government Terraforming（Venus使用時の太陽系フェイズ）が未実装
- 植民地トラックが世代終了時に上昇しない
- Turmoilの世界的イベントがログ表示だけで効果が発動しない
- 有料代表者（予備から5MC）が無料で送れる
- **Prelude ソロが14世代のまま**（監査は「公式は12世代」と指摘）。
  実装は `app/game-logic.js` の `generation >= 14` ハードコード1箇所で、
  Prelude の有無を見ていないのは**実測で確認済み**（`preludeEnabled` は追加済みなので判定は可能）。
  **ただし出典が取れないため未修正。** `Downloads/MARS拡張ルール/` にあるのは Prelude 2 の
  ルールブックだけで、そこに solo の記載は無い（テキストは改行なしの1行なので
  行単位のgrepではなく `grep -oiE ".{90}solo.{140}"` のような文字列検索で確認すること）。
  無印 Prelude のルールブックを入手してから直すこと。根拠なしに世代数を変えない
- イベントカードのタグが解決後も数えられる
- Prelude カードのタグが数えられていない

**修正前に必ず実測で再現すること。** 監査の指摘は精度が高かったが、
既に修正済みのものや、私の実装ミスと重複するものも含まれていた。

---

## 7. 開発の進め方

### コマンド

```bash
npm test                 # ビルド + 312テスト
npm run lint             # ESLint
npx tsc --noEmit -p tsconfig.json | grep '^app/'   # 型（worker/ の既存エラーは無視）
node scripts/playtest.mjs --games=20 --players=2 --colonies --turmoil
npm run deploy           # Workers へデプロイ
git push origin main     # Pages は Actions が自動ビルド
```

### ルールを追加・修正するとき

**必ず `app/game-command.js` に実装する。** 以下は禁止:

```
オンラインだけ修正 / ロボットだけ例外処理 / page.tsx だけボーナス追加
```

UI・サーバー・ボットは「何をしたいか」を送るだけ。
「それが可能か」「何が起きるか」を決めるのはエンジンだけ。

### 外部AIの利用

このプロジェクトでは以下を使った:

- **Codex**（`mcp__codex__codex_ask`）: カードアート428枚のSVG生成、
  カード名381枚の翻訳、PDF解析。ファイル書き込みができる
- **sol**（`ask_codex.ps1 -Model gpt-5.6-sol -Effort high`）: 設計相談。
  **`-Effort` が正しいフラグ名**（`-ReasoningEffort` は存在しない）。
  プロンプトが長いとタイムアウトするので、質問を3点程度に絞る
- **agy**: 今回は繰り返しエラーで落ちたため使えなかった

---

## 8. 参考資料の場所

```
C:\Users\takkun\Downloads\MARS拡張ルール\     公式ルールブックPDF 10冊
C:\Users\takkun\Downloads\TM_RULEBOOK_JPN-reprint2018w_全文書き起こし.txt
```

**PDFからのテキスト抽出には注意が要る。** フォントが数字を独自コードに
割り当てているため、素朴に抽出すると `5 cards` が `ffl cards` に化ける。
ToUnicode CMap を解析する必要がある（`scratchpad/pdfx.py` に実装済み）。

盤面データは**画像認識ではなくリファレンス実装から移植した**。
`github.com/terraforming-mars/terraforming-mars` の `src/server/boards/*Board.ts` を
curl で取得できる。既存のタルシス盤面と1行ずつ一致することを確認済みで、
この経路は信頼できる。
