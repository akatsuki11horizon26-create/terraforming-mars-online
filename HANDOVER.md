# 引き継ぎ書

最終更新: 2026-09-03 / ブランチ `main` / テスト667件 + E2E 10本

**E2Eは5本→9本。バックログの5シナリオがすべて埋まった**（2026-09-03、§6.10）。
新規は #2 タイル配置 / #4 ソロ版ビルドのゲート / #5 オンライン2画面。
**#5 は `wrangler` と Durable Objects を起動する別プロジェクト**で、
引き継ぎ書が「CI安定性は未検証」としていた点は**hosted runner で実際に通ることを確認した**
（15.4秒、`continue-on-error` に助けられた素通りではない。ログを読んで確認済み）。

**このセッションで実バグを2件掘り出して直した**。どちらも行動パネル絡みで、
どちらも「エンジンは正しく計算し、DOMに出なかった」型（§6.10）。

**オンライン通しドライバを追加**（`scripts/online-playtest.mjs`、§6.11）。
2席をWebSocketで駆動して全世代を約1.5秒で走る。8戦すべて終局・世代11〜12・
勝者は常に高得点側・エラー0・異常0。

作業ツリーはクリーン。`main` は `origin/main` と同一（`a6f4f86`）。
**両方の公開先に配信済み**（CIの緑ではなく、配信中のバンドルを直接grepして検証した）。

> **このセッションは測定ミスを2回やった。再発防止は §7.1 に書いた。**
> 1件は「オンラインは毎回同じ山札を配る」という**存在しないバグを報告した**もの。
> 原因は部屋コードの正規化（§2.9）。もう1件は**死んだサーバーを叩いて「修正後」と報告した**もの。
> どちらも「実測した」と言える形をしていたのに、測っていたものが違った。

**PR #1〜#11 すべてマージ済み。両方の公開先に反映を確認済み。**

**オンライン対戦は 2026-08-19 に初めて成立した。** それまで公開されたまま
一度も動いていなかった（§6.7・§6.8）。現在は本番で世代1〜11の完走を実測済み。

| PR | 内容 |
|---|---|
| #3 | Workers デプロイのCI化（公開先2つとも自動になった） |
| #4 | ロボット戦の研究フェーズ・デッドロック修正／bot テストの乱数依存を除去 |
| #5 | **マルチの勝者判定**（§3「勝敗はソロとマルチで別物」）／オンラインの勝敗表示／日本語化 |
| #6 | **データにあるのに誰も読んでいなかった配置・価格ルール6件**（§2.3.3） |
| #7 | ドラフトをどの卓でも選べるように／**手番ごとの行動パネル**（§6.4・§6.5） |
| #8 | 自分の手番のパネルを1.3秒に短縮／「自分」の判定を手番から座席へ |
| #9 | **生産量の変化をパネルに出す**（コストしか出ていなかった／§6.6） |
| #10 | **オンラインが起動即クラッシュしていた**のを修正（§6.7） |
| #11 | **オンラインが世代3に進めなかった**のを修正（§6.8） |

**#5・#6・#9・#10・#11 はすべて「間違って書かれていた」のではなく
「書かれていなかった」。** だからテストもCIも全部緑のまま長期間見逃されていた。
§2.3.3 の探し方と §6.6・§6.8 の検証方法が、この種を掘り出すための道具。

> **Pages のデプロイは 503 で2回落ちた。**（GitHub側の障害。ビルドは成功していた）
> このとき `gh` の GraphQL も落ちるので `gh pr merge` / `gh pr create` が使えない。
> **REST なら通る**: `gh api -X PUT repos/OWNER/REPO/pulls/N/merge -f merge_method=merge`。
> 復旧は **`gh run rerun` ではなく `gh workflow run pages.yml --ref main`**。
> rerun すると artifact `github-pages` が2個になり "Multiple artifacts named" で必ず落ちる。
>
> **「CIが緑」で終わらせず、配信中のバンドルを直接見て確認すること。**
> `curl -s <URL>/ | grep -oE '/assets/[^"]+\.js'` で資産を列挙し、
> 各ファイルを grep して新しい文字列が入っているか見る。
>
> **そしてバンドル検証でも終わらせない**（§6.6）。grep が答えるのは「届いたか」だけで、
> **動くかは答えていない**。本番URLを開いて実際に操作するまでが確認。
>
> **minify で検査文字列は変形する。** `data-mine="yes"` は引用符が外れて
> `[data-mine=yes]`、`0.6s` は `.6s`、関数名は消える。**0件でも慌てず、
> まず検査文字列の選び方を疑う**（CSSのキーフレーム名はJSバンドルには無い）。
>
> **ブラウザキャッシュにも注意。** デプロイ直後に「まだ直っていない」と見えたら、
> エラーのスタックに出るバンドル名と、配信中のバンドル名を突き合わせること。
> 食い違っていれば古い方を実行している（`?v=2` を付けるか Ctrl+F5）。

**ブラウザ実機確認は 2026-08-17 にはじめて通した。** ロボット戦を最後まで
（世代22・最終緑化・終局画面）動かし、**そこで勝者判定の欠落を発見した**
（テストもCIも全部緑のまま見逃していた）。以後、**「遊んで確かめる」を
このプロジェクトの標準手順にしている**（§7「作業の型」）。

このファイルは**着任した人が最初に読むもの**。
章の順序は「知らないと事故る順」で、作業の時系列ではない。

- **§1** これは何か
- **§2 このコードベース特有の罠** — ここを読まずに触ると壊す
- **§3** アーキテクチャの原則
- **§4** ファイル地図
- **§5** 実装済みの機能
- **§6 未対応・未確認の一覧** — 次にやること
- **§6.5 上流差分ハーネス** — いま育てている検証の仕組みと、その罠
- **§6.10 ブラウザE2E** — Playwright 9本（5シナリオ完了）。実バグを計4件掘り出した
- **§6.11 オンライン通しドライバ** — 2席のWSクライアントで全世代を1.5秒
- **§7** 開発の進め方
- **§8** 参考資料

---

## 1. これは何か

Terraforming Mars の非公式Web実装。公式ルール準拠を目指している。

**公開先は2つ。同じソースから別ビルドを出している。**

| 公開先 | URL | 内容 | 更新 |
|---|---|---|---|
| GitHub Pages | https://akatsuki11horizon26-create.github.io/terraforming-mars-online/ | ソロ・ロボット戦専用（サーバーなし） | `pages.yml`（main push で即） |
| Cloudflare Workers | https://mars-frontier.gameofai.workers.dev | 上記＋オンライン対戦 | `deploy.yml`（**Test 成功後**） |

> **片方だけ更新される事故に注意。** 以前は Workers 側にワークフローが無く、
> main にマージすると **Pages だけが更新されてオンライン版が取り残された**。
> CIが全部緑なので「公開された」と誤認しやすい。現在は両方とも自動化してある。

**2つのワークフローは意図的に非対称。** `pages.yml` は push で無条件に走るが、
`deploy.yml` は `workflow_run` で **Test の成功を待ってから**走る。
Workers 側は Durable Objects に**進行中の対戦部屋**を抱えており、
デプロイすると DO が再起動されるため、壊れたビルドを載せる代償が大きいから。

`deploy.yml` は `workflow_run.head_sha` を明示的にチェックアウトする。
**既定のままだとテストされたコミットではなくブランチ先端が出る**ので、
ここを消すと「Testが見ていないものをデプロイする」に戻る。

必要なシークレット: `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID`。

ソロ版は `NEXT_PUBLIC_SOLO_ONLY=1` でビルドされ、`ONLINE_ENABLED` が false になる。
**サーバーが存在しないので、ソロ版のUIはエンジンを直接呼ぶ。** これは設計上の意図であり、
「UIはルールを持たない」原則とは両立する（呼び出すだけで、ルールは持たない）。

### 技術構成

- Next.js 16.2.6 / React 19.2.6 / vinext / Vite 8 / TypeScript 5.9.3
- Cloudflare Workers + Durable Objects（1部屋1DO）、WebSocket
- テスト: `node --test` 662件 ＋ Playwright（E2E）5件
- CI: GitHub Actions（lint / 型 / 静的ビルド / **E2E** / テスト / 全拡張プレイテスト / 監査23本 / 5マップ）
  `test.yml` → `deploy.yml`（Workers）、`pages.yml`（Pages）の3本
- セーブ: `rulesVersion: 5`。v3・v4は読み込み時に自動移行

---

## 2. このコードベース特有の罠

**ここが一番大事。** 以下はすべて実際に踏んで、時間を溶かしたもの。

### 2.1 非列挙アクセサ（最重要）

`state.mc` は `players[currentPlayerId].mc` を指すアクセサ。**`Object.defineProperty` で
`enumerable: false` として張られている。**

```js
const next = { ...state };   // ← アクセサが消える。事故の最大の原因
```

正しくは `cloneGameState(state)` を使う。`viewForPlayer` でもこれを踏んで、
オンラインの全クライアントが「手番の人の手札」を読む状態になっていた。

**将来的には廃止して `players[id]` 明示に移行すべき**（sol の助言）。
ただし移行するなら、ロード境界で一度だけ変換し、
`state.mc ?? players[id].mc` のような恒久フォールバックは作らないこと。

**派生する罠**: `state.plants` のような単数形アクセサは*行動主*を指す。
「任意のプレイヤーの植物を除去」をこれで書くと**自分を攻撃する**（§2.5 の実例）。

### 2.1.1 「全員が同時に答える」場面でアクセサを使うと固まる

§2.1 の派生。**研究フェーズは全員が同時にカードを買う**が、
`currentPlayerId` は1人しか指さない。ここでアクセサを使うと、
**手番マーカーを持っていない人の画面が壊れる**。

実際に起きたのがこれ（ロボット戦で世代1終了後に操作不能）:

| 見ていたもの | 値 |
|---|---|
| `activeState.researchCards`（パネルが描画） | **ロボットの手札 = 0枚** |
| `players.find(HUMAN)`（駆動ループが待つ） | **人間の手札 = 4枚** |

第1プレイヤーマーカーは世代ごとに回るので、**世代2以降の研究フェーズは
ロボットが `currentPlayerId`** になる。駆動ループは「人間がまだ選んでいない」
と judged して停止し、画面にはカードが0枚。**双方が相手を待つデッドロック。**

`activeState.mc` も同じ理由でロボットの残高を見ていたので、
価格判定も壊れていた。

> **「今この画面を操作しているのは誰か」と「手番マーカーは誰か」は別物。**
> 研究・Prelude・企業選択のような同時進行の場面では、
> `players.find(id)` で**明示的に指名する**こと。
> 現在は `researchSeat` がその役をしている（`page.tsx`）。

### 2.2 テストが乱数を読んでいる

**このセッションだけで4回踏んだ。** 症状は「数回に1回落ちる」。

原因は3層あり、対策も3段階になる。

| 乱数源 | 症状 | 対策 |
|---|---|---|
| 企業がランダム（13社） | 初期資源・生産量が毎回違う | 差分で測る |
| **企業効果が値そのものを変える** | 差分でも落ちる | `corporationId` を固定 |
| 植民地タイルがランダム | 交易の**収入**が毎回違う | 初期値だけ違う2本の差を取る |
| **世界的イベントがランダム** | 開始時の dominant party が毎回違う | `globalEventOrder` で配りを固定 |

最後の1つは 2026-08-07 に足された経路。セットアップが引いた2枚のイベントが
中立代表者を配置し、それが開始時の dominant party を決めるため、
**盤面がシャッフル依存になった**。これで既存テスト5本が乱数を読む状態になり、
実行ごとに落ちる組み合わせが変わった。

`getInitialState({ globalEventOrder: [...] })` で配りを固定できる。
`tests/turmoil.test.mjs` の `pinnedTurmoilState()` がその形。
**Turmoil のテストで dominance を読むなら必ず固定すること。**
固定すると kelvinists に中立2個が乗った状態から始まるので、
「ある政党を優勢にする」には**3個**必要になる（空の政党を使うなら別）。

```js
// 悪い例 — 企業を引くたびに変わる
assert.equal(after.energyProd, 1);

// 良い例 — 差分
assert.equal(after.energyProd, before.energyProd + 1);

// 差分でも足りない例 — Beginnerを引くと研究が無料になる
state.players = state.players.map(p =>
  p.id === buyer ? { ...p, corporationId: "corp-teractor" } : p
);
```

**「支払い後の残高」は価格ではない。** Callisto は交易報酬に電力2を払うので
`5 - 3 + 2 = 4`。収入を伴う操作では、初期値だけが違う2本を比べれば収入が相殺される。

**「1ゲームを fork すれば固定」も嘘のことがある。** `getInitialState` を毎回呼ぶ
ヘルパーは、fork してもタイル自体が毎回違う。

> **ローカルで数回通っても「直った」証拠にならない。**
> 落ちる確率を見積もってから試行回数を決めること。
> 13社中1社なら約8%。6連続成功はただの運で、実際にCIでだけ落ちた。

### 2.3 「成功を返すが何もしない」

エラーを出さず、条件が偽になるだけのコードが繰り返し出てくる。

- `applyCardAction` が `cardId` を渡さず、資源が置かれない
- `getCardPlayableStatus` は「コストと条件」の判定で、**所持を見ない**。
  手札0枚でも428枚中272枚が `playable: true` を返す
- `computeScore` は該当カードを見つけられなければ**黙って加算しない**

**得点計算は特にこの性質が強い。** 実際に4枚が0点になっていたが、
例外もログも出ていなかった。**合計点だけ見ても妥当な値に見える。**

> カードを1枚ずつ「置いた場合と置かない場合の差」で測らないと発見できない。
> 「テストが通っている」も「例外が出ない」も、この種のバグの証拠にはならない。

#### 2.3.2 「ソロ用の判定がマルチにもそのまま効いている」

同じ一族の最上位。**書かれていない処理は、エラーも出さずに正しく見える。**

終局判定は `isGameOverCheck(temperature, oxygen, oceans)` ——
つまり「火星がテラフォーミングされたか」だけを見ていた。これは**ソロの勝利条件**で、
マルチではただの**制限時間**でしかない。相手より点が低くても
`MISSION SUCCESS` と出る状態が、**426件のテストとCI全緑のまま**残っていた。

実際に見つかったときの盤面は 68点 対 20点。負けている側に成功と表示された。

> **「誰も比較していない」は grep では見つからない。**
> 褒賞には順位付けがあるので「順位の概念はある」ように見えるが、
> ゲームそのものの勝敗を決めるコードは**存在しなかった**。
> 無い処理を探すには、コードではなく**最後まで実際に遊ぶ**しかない。

派生: `gameResult` は「**この画面を見ている人が勝ったか**」を意味する。
エンジンは1つしか書けないのでローカル席の答えを書く。
オンラインでそれをそのまま全員に配ると、**勝者に敗北と表示される**。
`viewForPlayer` が受信者ごとに `winnerPlayerIds` から導き直す（§3）。

### 2.3.1 候補0の choice は「詰み」であって「無効」ではない

`pendingChoice` があると他の操作は全部ブロックされる（§3）。
そこに**選択肢が1つも無い choice** を置くと、答える手段が無いまま
盤面が固まる。例外もログも出ない。§2.3 の一族。

**盤面のマスを答えにする choice は特に危ない。** UI は
`options[].targetCellKey` が指すマスだけを光らせるので、
`options: []` は「光るマスが1つも無い」＝**クリックで答えられない**を意味する。

`buildTileChoice` は合法マスが0なら `null` を返す。**この null を握り潰して
自前の choice オブジェクトを組み立てないこと。** 実際に v3 セーブの移行が
`options: []` を直接書いており、復元したゲームが操作不能になっていた。

### 2.3.3 データにあるフィールドを、誰も読んでいない

2026-08-18 に見つけた4件。**すべて「間違って書かれていた」のではなく
「書かれていなかった」。** §2.3 の一族で、テストは全部緑のままだった。

| フィールド | どこにある | 誰も読まないと何が起きるか |
|---|---|---|
| `effect.tilePlacementRule` | カードの `tile.on` を parse した結果 | 置き場所を指定する10枚が**盤面全部**を候補に出す |
| `cell.placementCost` | ヘラス南極（6 M€） | **タダで置ける** |
| `ocean-tile` 配置ボーナス | ヘラス南極 | `grantPlacementBonus` の汎用枝で**黙って消える**（`field in player` が false） |
| `noVolcanicRestriction` | ヘラス・ユートピア | 火山ルールを効かせると溶岩流が**どこにも置けなくなる** |

**一番効くのはモホール地帯。** `on: "ocean"` なので海洋予約マスに置く
カードだが、特殊タイルは「陸地のみ」に制限されるので、
**提示された44マスのうち合法なマスは0だった**。置き場所を絞る話ではなく、
**そもそも正しく置けないカードだった**。

**`noVolcanicRestriction` は §2.3.1 の実例でもある。** 火山ルールを実装した
直後、ヘラスとユートピアで溶岩流の合法マスが**0**になった。この2マップには
火山マスが無い。生成スクリプトのコメントが理由を書いている——
「Hellas has no volcanoes ... so those cards lose their placement restrictions here」。
**制限を追加するときは、その制限が空集合を作るマップが無いか必ず数える。**

**探し方**（この4件はこれで見つけた）:
データ側のフィールド名を grep して、**代入している行しか出てこないなら
誰も読んでいない**。`tilePlacementRule` は `game-logic.js:652` の1行だけ、
`noVolcanicRestriction` は `BOARDS` の定義だけだった。

**同じ探し方で、さらに2件出た（2026-08-18）**:

| 症状 | 原因 |
|---|---|
| 建材・チタンの価値を上げる3枚（先進合金・レゴ樹脂・水星合金）が**無効** | 建材は `* 2` が3箇所にベタ書き、チタンは**企業効果しか見ていなかった** |
| 統一党が与党のとき、チタンの価値が上がらない | `hasPolicy` を呼んでいるのは `trSurcharge` だけだった |

> **カタログの綴りは `titanumValue`**（`i` が無い）。リファレンス実装の
> タイポがそのまま来ている。**水星合金はこの綴りしか持っていない**ので、
> 両方の綴りを見ないと動かない。
>
> **与党の政策は `policies[0]` しか選ばれない**（`turmoil.js:317`）。
> 6つある passive のうち**ゲーム中に到達できるのは2つだけ**
> （赤の党 `trSurcharge`・統一党 `titaniumValue`）。
> 残り4つが未実装なのは、**今のところ実害が無い**。
> 政策を選べるようにするなら、ここが未実装であることを先に思い出すこと。

**「データを検査するテスト」は実装を検査しない。**
`the Hellas south pole charges to place and pays an ocean` は
`pole.placementCost === 6` を assert していた。**盤面データがそう言っている**
ことは確かめていたが、**誰かが実際に課金するか**は一度も見ていなかった。

### 2.4 「二重に見える」と「二重に動く」は別

同じ効果が2箇所にあるのを見つけたとき、**実測と到達性の両方**を確かめる。

実例: `placeTileAt` が Tharsis に mcProd+1/MC+3 を払っており、`page.tsx` にも
同じブロックがあった。実測すれば「二重取りになる」。しかし到達性を調べると
`placementMode.type` は `"forest"` 固定で、`type === "city"` になる経路が無かった。

- 実測だけで消す → 生きたコードを削る危険
- 到達性だけで消す → 死んだコードを残す

### 2.5 攻撃カードは対象を「選ばせる」必要がある

「任意のプレイヤーから〜」は、**必ず pending choice を経由**する。
直接適用すると単数形アクセサ経由で行動主に当たる（§2.1）。

実装は共通経路にまとまっている:

| 種別 | choice kind | ledgerの種別 |
|---|---|---|
| 資源の除去 | `resource-attack` | `resource-removal` |
| 資源の奪取 | `resource-steal` | `resource-removal` |
| カード上の資源の除去 | `resource-steal`（`targetCardId` 付き） | `resource-removal` |
| 生産量の低下 | `production-attack` | `production-decrease` |

**ソロは例外。** 相手がいないので直接適用する。
`players.length > 1` で分岐しており、これを外すと**ソロで二重適用**になる。

**ただし `resource-steal` にはこの分岐が無い。** ソロでも choice を出す。
相手の手持ちを狙う選択肢は候補0になって自然に消えるが、
Virus の動物のように**自分のカードが正当な対象になる**効果は
ソロでも選択が必要だからで、これは意図的。ここに `players.length > 1` を
足すと**ソロで Virus が何もしなくなる**。

実際に減った場合だけ `generationAttackLedger` に記録される。
自己攻撃・対象資源0・任意効果の辞退では記録しない（Law Suit の対象判定に使うため）。

### 2.6 `npm test` はビルドを挟む

`npm test` は `npm run build` を実行してからテストする。
ビルド中に走ると古い成果物を読んで落ちることがある。

**1回の失敗では判断しない。3回連続で確認すること。**

`static-dist/index.html` を読むテストが2件ある。ローカルには過去のビルドが
残っていて通るが、CIには無い。CIには `npm run build:static` を入れてある。

### 2.7 効果スペックはキーのホワイトリスト

`normalizeBehavior` は既知のキーだけを拾う。**未知のキーは黙って捨てられる。**

新しい挙動を足すときは:

- `card.effectSpec.behavior` に書く → normalize を通る（キーの追加が必要）
- `card.effect` に書く → **normalize を素通りする**（`getCardEffect` の1行目）

St. Joseph の `buildCathedral` は後者を使っている。
Flooding は `{tr:{oceans:1}}` という誰も読まない形で書かれていて、
**海洋を1枚も置いていなかった**。

### 2.8 カード資源の種別は別ファイルにある

**カタログ本体に `resourceType` は入っていない。** 動物・微生物・フローターなど
「カードの上に乗る資源」の種別は `app/card-resource-types.js`（72枚、生成物）にある。
リファレンス実装の `resourceType` 宣言から
`scripts/generate-card-resource-types.mjs` が吸い出したもの。

```js
collectResourceTargets(state, "animal", ALL_CARDS, {
  mustHaveResources: true,
  getResourceType: getCardResourceType   // ← これを渡さないと1枚も見つからない
});
```

**`getResourceType` を渡し忘れると、例外ではなく「対象0枚」になる**（§2.3の類型）。
`collectResourceTargets` は `card.resourceType` を先に見て、無ければこの関数に訊く。
両方空振りすると黙って候補から外れる。

置く方向（`addResourcesToAnyCard`）と取り除く方向（Virus）で同じ関数を使う。
Microbe / Science / Floater / Asteroid なども同じ経路で書ける。

**カードの上の資源とプレイヤーの手持ちは別物。** `resource-steal` の選択肢は
`targetCardId` があればカード上の資源、無ければ手持ちを指す。分岐を消すと
Virus が相手の植物を消す側に落ちる。

### 2.9 部屋コードは5文字に正規化される（存在しないバグを1件でっち上げた）

`normalizeRoomCode` は**大文字化 → 英数以外を除去 → 紛らわしい文字を写像 →
先頭5文字**まで刈り込む。長い「ユニークな」文字列は**全部同じ部屋に落ちる**。

```
TMTL7V2EW -> TMTL7
TMTL7V2JZ -> TMTL7      ← 別コードのつもりが同じ部屋
TMTL7V2P5 -> TMTL7
PROBEX1   -> PRQBE      ← O が Q に写像される
```

そして**Durable Object は前のゲームを復元する**ので、2回目以降は
「新しい部屋を開いたのに前回の続きが返ってくる」。

> **これで実際に誤報を1件出した。** 「毎回ユニークなコード」で8戦回して
> 全部バイト単位で同一の結果が返り、**「オンラインは毎回同じ山札を配る」**と報告した。
> 実際には8戦とも同じ1部屋で、DOが同じ状態を返していただけ。
> 無修正のサーバーで正しいコードを使えば、seedも企業も部屋ごとに違う。
>
> **「独立した試行のつもり」が独立していない**のは、結果が「安定している」
> ように見えるので気づきにくい。**同一すぎる結果は成功ではなく異常**と読むこと。

`scripts/online-playtest.mjs` は正規化で変形するコードを**実行前に弾く**
（`normalizeRoomCode(CODE) !== CODE` なら何になるかを言って終了する）。
新しくドライバを書くときも同じガードを入れること。

---

---

## 3. アーキテクチャの原則

**`app/game-command.js` が唯一の状態変更入口。**

```
オンライン: UI ──command──> Durable Object ──> executeGameCommand
ソロ:      UI ────────────command────────────> executeGameCommand
Bot:       ────────────────command────────────> executeGameCommand
```

ここが引き受けているもの:

- 手番検証・フェーズ検証・所持検証
- **アクション消費**（成功時のみ1回。選択を伴う場合は最後の選択時に1回）
- 企業トリガーとパラメータ閾値ボーナス（`afterPlay` continuation）
- pending choice がある間の他操作のブロック

### ルールを追加・修正するとき

**必ずコマンド層かエンジンに実装する。** 以下は禁止:

```
オンラインだけ修正 / ロボットだけ例外処理 / page.tsx だけボーナス追加
```

UI・サーバー・ボットは「何をしたいか」を送るだけ。
「それが可能か」「何が起きるか」を決めるのはエンジンだけ。

この原則を破った結果として、同じ「カードをプレイする」処理が4箇所に
別実装で存在し、片方だけ直したバグが繰り返し再発していた（配置ボーナス二重加算、
TR加算漏れ、ロボットの海洋二重加算など実際に4件）。**移行は完了している。**

### 得点計算は二段構成

`app/scoring.js` が、**ゲーム全体から寄与を生成してからプレイヤーごとに合算する**。

```js
buildScoreContributions(state, options)  // 全寄与を一度に生成
calculateScoreBreakdowns(state)          // プレイヤーIDごとの内訳
computeScore(state, playerId)            // 既存呼び出し互換のラッパー
```

この形でないと「**他人に点を与えるカード**」が原理的に書けない
（Law Suit は相手に−1VP、Vermin は全員に−VP）。

- `points` は負数を許可。**合計は0で丸めない**
- 得点を受ける人物は必ず `targetPlayerId` で指定する
- カード所有者と得点対象者は分離されている

### 勝敗はソロとマルチで別物

**ソロ**は火星が相手なので、3トラック（気温8・酸素14・海洋9）で決まる。
**マルチ**では3トラックは「制限時間」でしかなく、**勝敗は得点**で決まる。

`FINISH_FINAL_GREENERY` が最後の1人を処理した時点で:

```js
standings        // 得点降順、同点はMC降順。ソロでは null
winnerPlayerIds  // 同点かつ同MCなら複数（分け合い）。ソロでは null
gameResult       // 「この画面を見ている人が勝ったか」
```

- **`gameResult` は視点つきの値**。エンジンはローカル席の答えを書き、
  `viewForPlayer` が受信者ごとに `winnerPlayerIds` から導き直す（§2.3.2）
- **`standings` / `winnerPlayerIds` は公開情報**。終局後の得点は全員が見てよい
- 古いセーブは両方持たない。`save-migration.js` が `null` で埋め、
  終局画面はソロ用の文言へフォールバックする

### タグを数えるとき

**`countActiveTags(state, playerId, tag)` を使う。** 数えるのは:

```
企業タグ ＋ 場に残る緑・青カード ＋ 選択済みPrelude
```

赤イベントは解決済みなので**数えない**（`playedEvents` へ移る）。
以前は逆で、イベントのタグが永続し、Preludeのタグは無視されていた。

**ただしイベントの「枚数」を数えるものは別。** Legendマイルストーン（イベント5枚以上）は
`playedEvents` と `playedProjects` の**両方**を読む。旧セーブはイベントが
`playedProjects` に残ったままだからで、片方だけ読むと過去のセーブで0枚になる。

### UIに残る直接変更

```
.mc  ±=   0箇所
.tr  ±=   0箇所
Prod ±=   0箇所
```

**資源・TR・生産量への直接代入は全廃した。盤面への直接書き込みも0箇所。**
最終緑化のTR巻き戻し（かつて `nextState.tr -= 1` と書かれていた1行）は
`placeTileAt(..., { finalGreenery: true })` としてエンジンへ寄せてある。
酸素もTRも「上げてから戻す」のではなく、**最初から上げない**。

盤面クリックは `pendingChoice` が名指しした合法マスを答えるだけになった。
`tileChoiceCells` が無いマスは**すべて操作不能**で、クリックは説明を出すだけ。

---

## 4. 主要ファイル

| ファイル | 役割 |
|---|---|
| `app/game-command.js` | **唯一の状態変更入口**。ここにルールを追加する |
| `app/game-logic.js` | エンジン本体（約3000行）。カード効果・盤面・フェーズ |
| `app/scoring.js` | 得点計算。寄与生成と集計（§3） |
| `app/pending-choice.js` | 選択肢の組み立て。`continuation` は明示列挙 |
| `app/official-content.js` | カタログへの上書き。生成物を直接編集しないための層 |
| `app/page.tsx` | UI。ルールを持たない |
| `worker/room.ts` | Durable Object。認証・配信・永続化。ルールを持たない |
| `app/bot-player.js` | ボットAI。合法手を列挙して得点差で評価 |
| `app/player-state.js` | アクセサ互換層（§2.1） |
| `app/save-migration.js` | セーブ移行。`CURRENT_RULES_VERSION = 5` |
| `app/net-protocol.js` | 通信形式と `viewForPlayer`（プライバシーフィルタ） |
| `app/board-milestones.js` | マップ別の称号・褒賞 |
| `app/colony-text.js` | 植民地タイル効果の日本語。**生成物 `colony-tiles.js` を直接編集しないための層** |
| `app/global-events.js` | 世界的イベント36枚の効果スペック（宣言的。§6.3に未対応8枚） |
| `app/full-card-catalog.js` | **生成物。直接編集しない**（`official-content.js` 経由） |
| `app/card-resource-types.js` | **生成物**。カード上の資源種別72枚（§2.8） |
| `app/card-art.data.js` | 428枚のカードアート（生成物、689KB） |

### 生成スクリプト

```
scripts/generate-boards.mjs        4マップ盤面（リファレンス実装から移植）
scripts/generate-japanese-text.mjs カード名・効果の日本語
scripts/build-card-art.mjs         SVGアートの検証とバンドル
scripts/check-card-art.mjs         主題が中央にあるかの機械検査
scripts/generate-card-resource-types.mjs  カード上の資源種別（§2.8）
scripts/playtest.mjs               完全なゲームを回して不変条件を検査
```

---

## 5. 実装済みの主な機能

- **カード428枚**（基本・Colonies・Prelude・Prelude2・Promo・Turmoil・Venus）
  - 全カード日本語名。Venus は「金星」表記で統一
  - 未実装効果 **0枚**（テストで検証。1枚でもあれば落ちる）
- **カードアート428枚**（SVG、Codex生成）
- **5マップ**（タルシス・ヘラス・エリシウム・ユートピア平原・アマゾニス平原）
  - 各マップ固有の称号5種・褒賞5種
- **ドラフト制**（ロビーで選択。初期10枚も対象。世代ごとに向きが反転）
- **ロボット戦**（3難易度。得点差で評価）
  - **合法手はエンジンが列挙する**（`getLegalCommands`）。ボットは候補を
    自前で組み立てず、ここから選ぶだけ。称号・褒賞・標準プロジェクト・植民地・
    代表者派遣は**実際に `executeGameCommand` を試して**、`ok` かつ状態が
    変わったものだけを合法手とする（「成功を返すが何もしない」を弾くため。§2.3）
  - `legalCardPayment` が鋼材・チタンの支払いを総当たりし、
    **最小の資源消費**になる組み合わせを選ぶ。以前は `steel=0, titanium=0`
    でしか判定しておらず、**鋼材でしか払えないカードが「出せない」扱い**だった
- **最終緑化フェーズがコマンド化されている**
  `CONVERT_FINAL_GREENERY` / `FINISH_FINAL_GREENERY`。UI が植物を引いて
  タイルを置く経路は無くなり、パス順の巡回と終局判定もコマンド層にある
- **オンライン対戦**（部屋コード、5拡張＋5マップ選択）
- **特殊得点カード3枚**（Law Suit / Vermin / St. Joseph）— 得点・効果とも完全実装
- **攻撃カード7枚**（Hired Raiders / Sabotage / Virus / Air Raid /
  Special Permit / Comet for Venus / Flooding）— 対象選択と履歴記録
  - Virus は「任意のカードから動物2」「任意のプレイヤーから植物5」の
    **両分岐とも実装済み**。1つのダイアログに両方が並ぶ（§2.8）
  - Special Permit はグリーン与党を立てた実プレイ経路でテスト済み
- **Solar Phase**（公式の順序で1本化、中断可能）
  1. Game End Check → 2. World Government Terraforming（Venus有効時）
  → 3. Colony production（交易船返却＋全トラック+1）→ 4. Turmoil
  - WGT は**第1プレイヤーが選ぶ**（金星／気温／酸素／海洋）。
    `triggerProduction` が step 2 で停止して `pendingChoice` を置き、
    `finishSolarPhase` が残りを再開する。TR・配置ボーナスは与えない
- **Turmoil 最終得点**（党首1VP・議長1VP、中立は加点なし）
- **世界的イベントは31枚デッキ**（拡張が有効ならColonies/Venus分の5枚が加わる）
- **選択キュー**（`pendingChoiceQueue` + `phaseContinuation`）
  複数プレイヤーへ順番に質問できる。セーブ・再接続を跨いで再開する
- **グローバルパラメータの共通経路**（`applyGlobalParameterChange`）
  誰が上げても盤面側の閾値（酸素8%→気温、0°C→海洋）が発火し、
  `grantTr` で「上げた人への報酬」だけを切り分ける
- **盤面を見るカード**（`ARCTIC_ALGAE_ID` ほか、テーブル駆動）
  Arctic Algae は誰の海洋でも、Aphrodite は誰の金星上昇でも発火する
- **Turmoil の世界的イベント36枚**（`app/global-events.js` の宣言的スペック）
  - 影響力を数え、「数えるものは最大5」の上限を持つ
  - 損失は影響力で軽減してから適用する
  - **`effectText` を文字列解析しない。** 表示用の日本語であり、
    そこから挙動を復元するのは推測になる
  - `missingGlobalEventEffects` が空であることをテストで担保。
    スペックの無いカードを足すと落ちる

---

## 6. 未対応・未確認の一覧

**ここが次にやること。**

### 6.1 ブラウザ確認の到達点

**テストが緑なのは前提であって到達点ではない。** このプロジェクトで見つかった
重大な欠陥は、ほぼ全部が**テスト全緑のまま**残っていた（§2.3.3・§6.6・§6.8）。
下表が「実際に動かして確かめた範囲」で、**ここに無いものは未確認**。

| 対象 | 状態 | 根拠 |
|---|---|---|
| ソロ（全拡張） | ✅ | 2026-08-17 に終局まで |
| ロボット戦 | ✅ | 世代22・最終緑化・終局画面まで |
| 選択ダイアログ系 | ✅ | 2026-08-07（世界政府・Corrosive Rain 他） |
| 5マップの描画 | ✅ | 各61セル・全マップ描画内容が異なる |
| ホットシート | ✅ | 座席交代・候補企業の切替を確認 |
| 攻撃カードの対象選択UI | ✅ | 実描画とクリック解決を確認 |
| カットイン／行動パネル | ✅ | §6.5 |
| **オンライン対戦（標準プロジェクトのみ）** | ✅ | 本番で世代1〜11の完走（§6.8） |
| **オンラインでカードを打つ通し** | ✅ | 世代19・**カード91枚**・タイル配置29回で終局 |
| **オンライン＋全拡張** | ✅ | 世代32で終局。金星・植民地・動乱・プレリュード・プロモの choice が実発火 |
| **オンライン＋別マップ** | ✅ | ヘラス(25世代)・エリシウム(28世代)とも終局 |
| **オンライン3人＋ドラフト＋全拡張** | ✅ | 世代17で終局・**ドラフト327回**・3人の順位が正しく並ぶ |
| **ビューの秘匿（全局面の機械監査）** | ✅ | 下記。**修正を外すと13箇所検出**することも確認 |
| **オンラインのロビー〜卓（実ブラウザ2枚）** | ✅ | E2E #5。CI（ubuntu-latest）でも通る |
| **タイル配置・海洋・TR・カットイン** | ✅ | E2E #2 |
| **ソロ版ビルドのオンライン封じ** | ✅ | E2E #4。ゲート2箇所とも赤にして確認 |
| UIから通しでドラフト | ❌ 未確認 | エンジンとオンラインは検証済み（§6.4） |
| **オンラインの切断復帰** | ✅ | `scripts/online-reconnect-check.mjs`（§6.12）。通信量は未計測 |

**2026-08-19 に WebSocket ドライバでオンラインを広く通した。** 実際に発火した
choice: `tile-placement` / `resource-steal` / `resource-attack` /
`production-attack` / `discard-card` / `any-card-resource` / `world-government` /
`colony-placement` / `ocean-removal` / `corrosive-rain` / `event-discard` /
`standard-resource-pick`。**ここでドラフトキューの情報漏洩を見つけた**（下記）。

> **ドライバを書くときの注意（実際に踏んだ）**
> - **カードには要件がある。** 手札の先頭を無条件に送ると
>   「タグが足りない」で拒否される。`getCardPlayableStatus` で絞ること
> - **プレリュードは全員同時ではない。** `advanceSetupTurn` が
>   1人ずつ `setupStep: "prelude"` を立て、`applyPreludes` は他の席を拒否する。
>   **マークが立つのを待つ**（これは仕様。§2.1.1 とは別物）
> - **同じ部屋コードを再利用すると前回のゲームが復元される**（DOが永続化している）。
>   毎回新しいコードを使うこと

<details><summary>これまでの実機確認の詳細（時系列）</summary>

**2026-08-07 に選択ダイアログ系はブラウザで確認済み。**
世界政府（4択・海洋の合法マス強調・配置してTR/ボーナスなし・Solar Phase再開）、
Paradigm Breakdown（2人ぶん4問を消化）、Corrosive Rain（MC/カード2枚の3択）、
Dry Deserts（海洋除去→資源2回）を実際にクリックして通した。
**ここで1件バグを見つけて直した**（ホットシートの席が質問の持ち主に移らない、下記付録）。

**2026-08-17 にロボット戦を最後まで実機で通した。** ここで確認できたもの:

| 対象 | 結果 |
|---|---|
| タイトル → ロボット戦 → 拡張選択 → 相手設定 → 開始 | 通る |
| 企業選択（CrediCor で MC 42→57） | 効果が乗る |
| 初期カード購入（2枚6MC、57→51） | 価格・請求先とも正しい |
| **世代2以降の研究フェーズ** | **人間の提示4枚が出る。購入は人間に請求される**（旧: 固まる） |
| 盤面・タイル配置 | 緑地／都市／海洋が隣接規則どおり描画される |
| カードアート428枚 | 手札に出る |
| 「パス (この世代を離脱)」の文言 | アクション未実行時に正しく出る |
| **最終植物緑化フェーズ** | 出る。**植物0では「緑地を配置する」が disabled**（8植物ルールがUIで効いている） |
| 終局画面 | 出る（**ただし勝敗が間違っていた** → PR #5） |

**同じ日に、全拡張ONのソロでも確認した:**

| 対象 | 結果 |
|---|---|
| ソロ初期値 | TR 14・世代 1/14・中立タイル4枚が公式手順どおり置かれる |
| 金星トラック | 4つ目のパラメータとして出る（☀ 0%） |
| **動乱パネル** | 6政党・代表者数・議長・影響力・与党/優勢・現行/次回/予告が出る。**世代1は「現行 —」**（公式どおり3枚目を配らない） |
| **植民地パネル** | 5タイル・7段トラック・入植/交易ボタン。**効果文が英語だった** → 翻訳（`colony-text.js`） |
| **PRELUDE選択** | 4枚から2枚。**カード名が英語だった** → 70枚を翻訳 |
| **称号／褒賞パネル** | 5+5、進捗つき（テラフォーマー 14/35）。**褒賞を設立すると 30→22 MC、次は14 MC** と正しく上がる |
| 標準プロジェクトパネル | アクションフェーズ外では「アクションフェーズでのみ実行できます」 |

**オンライン対戦は動く（2026-08-19 修正）。** §6.7 参照。
§6.1 の「オンライン2端末が未確認」は、**2タブで検証して解消した**。

> **2026-08-18: 残り5件はブラウザで確認済み**（dev サーバー実機）。
>
> | 項目 | 結果 |
> |---|---|
> | **4マップの描画** | 全5マップとも `.hex-grid` に**61セル**描画・海洋予約12・全セルに `aria-label`。**5マップすべて描画内容が異なる**（盤面の使い回しは無い） |
> | **ホットシート** | 2人で起動 → `mode:"hotseat"`・TR20・名前入力欄2つ。企業確定で `currentPlayerId` が `player`→`player2` に移り、**候補企業も相手のものに切り替わる** |
> | **ドラフト** | エンジンは正常（世代1=右回り / 世代2=左回りで反転、3人で完走・キュー残0・全員10枚）。**ただし §6.4 参照** |
> | **攻撃カードの対象選択UI** | 小惑星をプレイ → 「植物を最大3個取り除く対象を選んでください。」＋**3人分のボタン（植物数つき）が実描画**。クリックで対象の植物 5→2、choice も解消 |
> | **カットイン** | 「気温 -28℃ → -26℃」が `.param-cutin` に描画される。**約1秒で消える**（`page.tsx:600`）ので、観測は 120ms 間隔で拾うこと |

</details>

### 6.2 未実装・未検証のルール

2026-08-07 の再監査で、この表にあった項目はすべて公式ルールブックと突き合わせて
**修正済み**。以下は当時の指摘と結果の対応。

| 項目 | 結果 |
|---|---|
| World Government Terraforming（Venus） | 実装済み（Solar phase step 2） |
| 植民地トラックが世代終了時に上昇しない | 確定バグ→修正（`advanceColonyProduction`） |
| Turmoil の世界的イベントが効果を発動しない | 確定バグ→36枚すべて実装 |
| 有料代表者（予備から5MC）が無料で送れる | 確定バグ→修正 |
| Prelude ソロの世代数 | **12世代に修正**（出典が見つかった、下記） |

再監査で新たに見つかり、あわせて直したもの:

- **旧与党のボーナスが支払われていた**。`runTurmoilPhase` は `advanceTurmoil` の
  *前* にボーナスを払っていた（変数名も `outgoing`）。公式は 3a で与党交代 →
  3b で**新**与党のボーナス
- **与党になった政党の代表者が盤上に残っていた**。leader 1個しか除去せず、
  次世代の dominance と influence が狂う
- **Party Leader の同数処理**。`computePartyLeader` が現職を受け取らず、配列順で
  先にいる方へ交代していた。配列順と現職が一致する並びでは素通りするので、
  テストは `["A","B","B","A"]` の並びでないと検出できない
- **世界的イベントの初期配置**。3枚目を current に配っていたが、公式は
  Coming と Distant のみ（"no CURRENT Global Event to execute the first generation"）

**Prelude ソロ12世代**: 前任者は「出典が取れない」として保留していたが、
出典は手元にあった。`Downloads/MARS拡張ルール/TM_RULEBOOK_JPN_図形原状保持版.pdf`
は**ファイル名に反して『プレリュード』のルール説明書 第5刷**である
（内部名 `_PRELUDE_RULES JPN reprint 202412.indd`）。p.3 に

> 『プレリュード』を使った１人プレイ
> １人ゲームに『プレリュード』を導入する場合、**12 世代**が終了するまでに、
> テラフォーミングを完了させてください。

とある。同ページの「地球化指数ソロ」にも
「期限は14世代（ただしプレリュード・カードを導入していれば12世代）」と重ねて書かれている。
数字はページ画像を3倍に拡大して目視確認済み（§8 の数字化けの件があるため）。

> **ファイル名から中身を推定して除外しない。** この1件は
> 「TM_RULEBOOK_JPN」という名前を基本セットのものと読んで開かなかったために、
> 監査2回ぶん保留され続けた。

なお同ページには未実装の**「地球化指数ソロ」バリエーション**
（全パラメータではなく TR63 以上、標準プロジェクト「緩衝ガスの導入」16MC）もある。

**Special Permit は実プレイ経路でテスト済み。** `createTurmoilState` で Turmoil を
立てて `rulingParty` を差し替えれば、通常のコマンド経路で最後まで通せる。
Turmoil 無し・別与党・グリーン与党の3通りで拒否理由まで確認してある
（`createTurmoilState` は既定でグリーンが与党なので、テストは意図を明示するため
与党を明示的に設定している）。

### 6.3 設計上の借り

**返済済み**（2026-08-14）:

- ~~最終緑化のTR巻き戻し~~ → `finalGreenery` フラグでエンジンへ（§3）
- ~~気温0°Cボーナスが `pendingOceans` と `pendingChoice` に分かれている~~ →
  **どちらも `pendingChoice` に統一**。`applyParameterThresholds` と
  `checkParameterThresholds` の両方が `buildTileChoice` を作るようになり、
  `openOrEnqueuePendingChoice` が「既に選択中ならキューへ回す」を引き受ける

- ~~`pendingOceans` の廃止~~ → **アクセサ・UI経路とも削除済み**（`f10cf9b`）。
  海洋の無償配置は**すべて `pendingChoice` 一本**になった。
  `save-migration.js` の v3→v4 移行だけが `pendingOceans` の名を知っている
  （**古いセーブが持っているので消せない**）。移行は `buildTileChoice` で
  合法マスを埋めるようになった。**候補0の choice はもう作られない**
- ~~`pendingUnownedOceans`~~ → 削除。初期化されるだけで誰も増やしていなかった

**残っているもの**:

- **非列挙アクセサの廃止**（§2.1）。`state.mc` 系は健在。
  **今回のデッドロックもこれが原因**（§2.1.1）。優先度は上がった
- `p-capital` と `card-base-capital` の重複エントリ。
  現在は後者が配られないため実害が無いが、**汎用分岐とハードコードの両方が存在する**。
  触るときは二重計上に注意
- **`gameResult` が視点つきの値であること**（§2.3.2）。
  エンジンとビューで意味が変わる唯一のフィールドで、危うい。
  **2026-09-03 に sol へ相談し、「いまは現状維持」という結論**。
  実害が出ていない一方、直すと保存形式を巻き込むため。

  将来やるなら**フィールドを消すのではなく分割する**:
  `missionResult`（ソロ専用の `"win"|"loss"|null`）を新設し、
  対戦の勝敗は UI が `winnerPlayerIds.includes(自席ID)` から毎回導出する。
  自席IDは online なら `viewerId`、ロボット戦なら `"player"`。
  **ホットシートには「自分」が固定で存在しない**ので、勝敗の見出しではなく
  勝者名を中立に出す。`rulesVersion` を6に上げ、v5以前の終了済みソロ保存の
  `gameResult` を `missionResult` へ移す移行が要る。
  **これをやらずにフィールドだけ消すと、古いソロ保存が全部「敗北」表示になる。**

  > 観戦・リプレイ・外部APIなど**状態の読み手が増えた時点**でやる価値が出る。
  > いまはコメント付きの現契約を維持する方が費用対効果が高い、というのが sol の判断。

---

### 6.4 ドラフトはどの卓でも選べる（2026-08-18 修正済み）

以前はオンラインのロビーからしか有効にできなかった。`multiplayer-lobby.tsx:239` に
チェックボックスがある一方、**`page.tsx` に `draft` の記述が1件も無く**、
ソロ・ロボット戦・ホットシートの設定画面はフラグを一切渡していなかった。
エンジンは最初から `playerCount > 1` で動いていたので、**UIの配線漏れだった**。

設定パネルに「カードの配り方」セクションを追加して解決。
**ロボット戦だけは条件が違う**: 最初のパネルでは人数がまだ1なので
`playerCount > 1` では出ない。`intent === "robot"` を条件に足してある。

**ホットシートの入口は依然として分かりにくい。** タイトル画面は3択
（ソロ/ロボット/オンライン）で、人数選択は**ゲーム中ヘッダの「新規ゲーム設定」**に
しか出ない（`intent === "custom"` のとき、`title-screen.tsx:139`）。
**タイトルからホットシートへ到達できるようになった**（2026-09-03）。
タイトル画面は4択になり、`mode-hotseat` が `intent="custom"` で人数2から設定画面を開く。
**ボタンを外すと `tests/e2e/title-modes.spec.ts` が赤になる**ことを確認済み。

> 実装もテストも前からあったのに、**入口が無いだけで長期間使えなかった**。
> 「実装済み」と「遊べる」は別物で、その差はエンジンのテストからは見えない。

### 6.5 「何が起きたか」は state.lastAction が持つ

数値の差分だけでは「値が動いた」ことしか言えず、**何が動かしたか**は言えない。
ロボットの手番では、原因の表示が無いまま数字だけが変わっていた。

`state.lastAction`（`{seq, playerId, playerName, kind, cardId, cardName}`）に
カードプレイ・標準プロジェクト・カードアクションの3経路が記録を残す。
画面はこれと差分を突き合わせて1手ぶんのパネルを出す。

**帰属で踏んだ罠が2つある。**

- **生産フェーズの収入を直前のカードに帰属させてはいけない。** 世代が変わると
  全員に収入が入る。これを直前の `lastAction` の効果として表示すると、
  「パテントの売却」の見出しの下に他人の収入が並ぶ。
  `phase === "production"` と世代跨ぎを除外している
- **`seq` は報告を実際に出したときだけ消費する。** 対象選択で止まるカードは、
  答えた後に数値が動く。プレイ時点で消費すると本命の変化を「古い」と判定して捨てる

**`prefers-reduced-motion` を切るときは `animation-name: none` を使うこと。**
短縮形の `animation: none` は効かない場合があり、点滅が止まらなかった。

**パネルが追跡している値は明示的な列挙である。** `readSeat` に書いた分しか
差分に出ない。資源7種と生産6種を見ている。**カード上のリソース（微生物・動物・
フローター）、植民地、議員数、特殊VP はまだ見ていない** ——
それらだけが動くカードは、今もパネルに「コストが引かれた」以外何も出ない。

### 6.6 「配信された」を「動いている」と書かない

2026-08-18、カードエフェクトを実装して**両公開先の配信JSに文字列が入っていることを
grep で確認し「反映済み」と報告した。ユーザーから「エフェクト出てない」と指摘された。**

実際に本番で遊ぶと、パネルは出ていた。ただし `発電所の建設 MC −11` だけで
**電力生産 +1 が無い**。差分検出が手持ち資源しか見ておらず、生産量を1つも
追跡していなかった。**このゲームは生産量を伸ばすゲームなので、生産カードは
「コストが引かれた」以外何も出ない** —— 遊ぶ側から見れば「出ていない」と同じ。

このとき **テスト432件・CI全緑・lint通過・型エラー増加なし・バンドル検証済み**
だった。それでも**使えば一目で分かる欠陥**が残っていた。

- **デプロイ後の確認は「その機能を実際に使う」まで。**
  バンドル検証は必要だが十分ではない。①配信物に入ったか → ②**本番URLで操作して
  期待した表示になるか**。②を省いて「反映済み」と書かない
- **表示系は代表的な入力で試す。** このときは小惑星（気温・チタンが動く）でだけ
  試して、**生産カードを一度も試していなかった**。そのゲームで最も普通の一手は
  何かを考えてから確認対象を選ぶ
- **差分を見せる機能では「何を追跡対象にしたか」を列挙してレビューする。**
  資源7種を入れて生産6種を落とした。列挙していれば気づけた（§2.3.3 と同じ形）

> **特殊VP3枚のバッジはカタログ側で確認済み**（`specialVictoryDisplay`）。
> 訴訟 `-1` / 聖ヨセフ `1` / 害虫 `特殊` の3枚だけが持っている。
> 画面に出るかは未確認。
>
> **カード面テキストの英語混入は0件**（428プロジェクト・70プレリュード・
> 49企業・36イベントの `effectText` を機械検査）。
> **企業名だけは英語のままが正しい**（CrediCor / Ecoline は固有名詞で、
> 日本語版もそう印刷している）。「全部日本語にする」テストを書くと誤って落ちる。

確認すべきもの:

| 対象 | 見るべきこと |
|---|---|
| タイトル → ソロ | 拡張選択パネルを経て開始できるか |
| タイル配置 | 合法マスが光り、クリックで置けるか。バナーが操作を遮らないか |
| カットイン | 中央に出て1秒で消えるか。盤面クリックを妨げないか |
| 購入フェーズ | 手札が見えるか（プレイは `WRONG_PHASE` で拒否済み） |
| 企業アクション | Ecoline / UNMI / Robinson のボタンが出て発火するか |
| 特許売却 | モードに入れるか |
| **pending choice 中** | 「先に選択を解決してください」が**操作不能に見えないか** |
| 特殊VP3枚 | カード面バッジが `-1` / `特殊` / `1` と出るか |
| 攻撃カード | 対象選択ダイアログが出るか |
| 最終得点画面 | 内訳が合計と一致し、負の値も表示されるか |
| **オンライン全般** | **テストで担保できていない**。2端末で企業選択・Prelude・研究購入 |
| **最終緑化**（08-14で経路変更） | 植物8で緑地が置けるか。**酸素もTRも動かない**か。終了ボタンで次の人に回り、全員終わると終局するか |
| **気温0°Cボーナス**（08-14で経路変更） | 選択ダイアログが出るか。**選択中に別のダイアログが来たらキューに並ぶ**か（旧経路は盤面クリックだった） |
| **ロボットの手の質** | 鋼材／チタンでしか払えないカードを**実際に出す**か（旧実装は出せなかった） |

#### 特殊カードの手動確認手順

エンジン側はテスト済み。以下は**画面に出るか**だけを見るためのもので、
値が合っているかはテストが担保している。すべて2人ソロ相当の卓で足りる。

1. **Law Suit** — 相手に攻撃カードを撃たせてから Law Suit をプレイする。
   対象選択に「今世代に自分から奪った人だけ」が並ぶか。
   選ぶと MC が最大3移動し、相手の得点が −1 されるか。
   *誰にも攻撃されていない世代では対象が0人になり、カードが出せないのが正しい。*
2. **Vermin** — 都市を置くたびに Vermin に動物が乗るか。
   二択アクションの両方が押せて、選んだ側だけが起きるか。
3. **St. Joseph** — 都市を選ぶダイアログが出るか。
   選んだ都市に大聖堂が描かれ、同じ都市が二度選べないか。
4. **特殊VPのカード面** — Law Suit `-1` / Vermin `特殊` / St. Joseph `1` の
   バッジが**カード表面**に出るか。
5. **最終得点内訳** — 負の VP が `-1` と表示され、合計と内訳が一致するか。
   **0で丸められていないこと**を必ず見る（丸めるのは実際にあったバグ）。
6. **オンラインの秘匿** — 2端末で、片方の pending choice の選択肢と手札が
   **もう片方の画面・DevTools のネットワークタブどちらにも出ない**こと。
   `viewForPlayer` のフィルタは過去に破れている（§2.1）ので、
   画面だけでなく**受信 JSON を直接見る**。
7. **Virus** — 動物を乗せたカードが場にあるとき、
   「動物2」と「植物5」が**同じ1つのダイアログに並ぶ**か。
   動物が1個しかないカードは「動物 1」と表示されるか。

### 6.7 オンライン対戦は2タブで検証する（クラッシュは修正済み）

**2端末は要らない。同じブラウザの別タブで検証できる。** ただし条件がある。

`playerId` は `localStorage` の `mars_frontier_identity` に保存された**1個のID**
（`use-room.ts:27 getIdentity`）。localStorage はタブ間で共有されるので、
**素直に2タブ開くと両方が同じ playerId を送り**、サーバーは
`members.find(m => m.playerId === identity.playerId)`（`worker/room.ts:223`）で
**同一人物の再接続とみなす**。結果、両タブとも「参加者 1/5・自分・部屋主」になり、
**同じ合言葉なのに相手が見えない**。バグではなく識別子の設計。

**回避手順**（これで参加者 2/5 になることを確認した）:

1. タブAで部屋を作る（この時点の ID で接続が確立する）
2. **タブBで `localStorage.setItem('mars_frontier_identity', 別の値)` してから参加する**
   —— A は接続済みなので後から ID を書き換えても影響しない

#### かつて「ゲーム開始」で両タブが真っ白になった（修正済み）

```
TypeError: Cannot read properties of undefined (reading 'length')
```

**原因は JSON 化で非列挙アクセサが消えること。**
`state.hand` / `state.mc` などは `players[currentPlayerId]` を読む
**非列挙**アクセサ（`player-state.js`）。非列挙にしてあるのは
セーブに古い値の写しを残さないためだが、**`JSON.stringify` も同じ理由で落とす**。

サーバーは `viewForPlayer` で丁寧にアクセサを付けて返すのに、
WebSocket を通った時点で全部消える。クライアント（`use-room.ts:93`）は
それをそのまま `setView` していたので、`page.tsx:2082` の
`activeState.hand.length` が `undefined.length` になって React がツリーごと落ちた。

**修正**: `net-protocol.js` に `hydrateView()` を足し、
受信直後に `withLegacyPlayerAccessors` を掛け直す。

> **`viewForPlayer` は「作る側」だけの関数だった。** ワイヤーの向こうで
> 元に戻す相棒が無かった。**送る側で加工したものは、受け取る側で戻す必要がある**
> —— テスト（`net-protocol.test.mjs`）は `viewForPlayer` の戻り値を
> **直接**検査していたので、JSON を通していなかった。だから14件全部緑のまま
> オンラインは一度も成立していなかった。
> 現在は `encode`/`decode` を通した往復を検査するテストがある。

**再現できなかった経路**: セーブ復元は使えない。`loadSavedState` が
`deck` の非配列を弾いて view を丸ごと捨てる。WebSocket 経由でしか再現しない。

**ローカル検証は `npm run dev` でよい。** `vinext dev` は Durable Object 込みで
動く（`/api/room/XXXXX/ws` が 426 を返せば生きている）。本番はminifyされていて
スタックが読めないので、**必ずローカルで再現させてから直すこと。**

### 6.9 ビューの秘匿は「隠し忘れ」で漏れる

**思いついたフィールドを個別に確認しても、漏洩は見つからない。**
手札・研究offer・企業/プレリュード候補・選択キューはすべて伏せてあったのに、
**ドラフトキューだけ伏せ忘れていた**（`...state` の素通り）。

現在は `tests/net-protocol.test.mjs` が**ビュー全体を機械的に走査**する:
他人の手札・山札・他人のドラフトキューに含まれるカードIDを集め、
**そのIDがビューのどこかに文字列として現れたら失敗**させる。
配るところ・ドラフト回覧中の2局面で検査している。

> **この監査は「検出できること」を確かめてから信用すること。**
> 最初に書いた版は進行後の状態だけを見ていて、そこでは
> `draft.queues` が空になっていたため**既知の漏洩を見逃した**。
> 修正を外して13箇所検出することを確認して、はじめて監査として成立した。

**ワイヤー形式に新しいフィールドを足したら、秘匿の要否を必ず判断する。**
画面には出ないが通信には乗っているので、**クリックでは絶対に見つからない。**


`viewForPlayer` は手札・研究offer・企業/プレリュード候補・選択キューを伏せていた。
**ドラフトのキューだけ伏せ忘れていた** —— `...state` の素通りで、
**全員が「相手がこれから引くカード」を読めた**。

ドラフトキューは**移動中の手札**（4枚から1枚取って残りを隣へ回す）なので、
手札と同じ扱いにしなければならない。現在は自分のキューだけが届き、
枚数は `queueCounts` で公開する（どこまで回ったかは秘密ではない）。

> **これは「送る側で加工したものを受け取る側で戻す」（§6.7）の裏返し。**
> あちらは**戻し忘れ**、こちらは**隠し忘れ**。
> **ワイヤー形式に新しいフィールドを足したら、秘匿の要否を必ず判断すること。**
> クリックでは絶対に見つからない（画面には出ないが、通信には乗っている）。

### 6.8 サーバーとエンジンで「手番」の定義がずれていた

オンライン対戦は接続もゲーム開始もできるのに、**世代3に到達できなかった。**
アクションフェーズが終わり研究フェーズに入り、片方がカードを買うと、
**もう片方は永久に買えず**フェーズが終わらない。

**原因**: `worker/room.ts` の `onAction` はすべての操作に
`currentPlayerId === seat` を要求していた。しかしコマンド層は
**企業選択・プレリュード・ドラフト・カード購入を手番から免除している**
（`game-command.js` の `SETUP_COMMANDS`）。公式ルールでこれらは**全員同時**に答える。
**同じルールを二箇所で別々に実装し、片方だけが正しかった。**

`pendingChoice` のガードも同じ問題を持っていた（他人の選択待ちで同時回答が塞がれる）。

**修正**: サーバーに `SIMULTANEOUS_ACTIONS` を置き、両方のガードで参照する。
`tests/online-validation.test.mjs` が、この集合がエンジン側の
`SETUP_COMMANDS` と一致することを検査する（`worker/` に単体テストが無いため、
ソースを読む形の検査）。

**見つけ方**: **クリックではなく WebSocket ドライバで全世代を自動進行させた。**
毎回きっかり世代2で止まるので、手で遊んでいるうちは
「なんとなく進まない」で終わっていた。
修正後は**世代11〜12で終局**する（気温8・酸素14%・海洋9、得点で勝者決定）。

> 手で2タブを操作していたときは、**企業選択フェーズまでしか見ていなかった**ので
> 気づけなかった。§6.6 の「代表的な入力で試す」と同じ穴。

#### オンラインを通しで検証するドライバ

`ws` パッケージで2席ぶんのクライアントを動かす。UI と同じ経路なので
実機確認として通用し、全世代が数十秒で終わる。骨組み:

```js
import { WebSocket } from "ws";
const ws = new WebSocket(
  `ws://localhost:3000/api/room/${CODE}/ws?playerId=${id}&name=${encodeURIComponent(name)}`
);
ws.on("open", () => ws.send(JSON.stringify({ type: "join" })));
ws.on("message", raw => {
  const m = JSON.parse(String(raw));
  if (m.type === "view") decide(m.view);   // 受け取った view だけで判断する
  if (m.type === "error") record(m.reason);
});
// 操作は { type:"action", action:"<COMMAND_MAP のキー>", payload:{...} }
// 開始は { type:"start", options:{...} }（部屋主だけ）
```

**書くときに踏んだ落とし穴:**

- **先に接続した側が部屋主。** 順番を固定しないと `start` が拒否される
- **view は連続で届く。** 同じ答えを二度送らないよう、
  `chooseCorporation` / `buyResearch` は一度きりのフラグで抑える
- **標準プロジェクトのIDは** `power-plant` / `asteroid` / `aquifer` /
  `greenery` / `city` / `convert-plants` / `convert-heat` / `sell-patents`
  （`game-command.js` の `STANDARD_PROJECTS`）。
  **上限に達したプロジェクトは `blocked` で拒否される**ので、
  気温8・酸素14・海洋9 に達したものは送らないこと
- 終局まで運ぶには `asteroid`（気温）→ `aquifer`（海洋）→ `greenery`（酸素）が最短
- 本番に向けるときは `wss://mars-frontier.gameofai.workers.dev/...` に変えるだけ

## 6.5 上流差分ハーネス（2026-09-02 現在）

上流実装（`terraforming-mars/terraforming-mars`、SHA
`1b26fe6989fe53c6a2a76cfe92f08eb9228f832f`）の**テストスイートを走らせて記録し、
同じ操作をこちらのエンジンで再生して照合する**仕組み。上流のソースを静的に
読むのではなく、実行して観測する。

静的抽出だと「同じカードに触れるテストが存在する」以上のことが言えない。
以前は「候補347件」としか言えなかったものが、いま**実測464件の全フィールド一致**。

### 数字（実測）

```
上流スイート  : 2,719 passing（記録中も維持することを毎回検証）
記録した操作  : 2,862（551カード / 1,760テスト）
再生して照合  : 464    一致 464 / 差分 0
名前付き除外  : 0件
```

**破壊確認**（これをやらない緑は信用しない）:

| 壊した箇所 | 赤になる件数 |
|---|---|
| 平坦な生産増加 +1 | 65 |
| `canAct` を常に false | 27 |
| `action` に余分な資源 | 9 |
| スコアラーのカード寄与 +1 | 62 |

### ファイル

| パス | 役割 |
|---|---|
| `scripts/build-upstream-differential.mjs` | 上流スイートを走らせ、551カードのprototype 6メソッドをラップして記録。**specは1行も編集しない** |
| `scripts/audit-upstream-differential.mjs` | 記録を再生して照合。差分があれば exit 1。CIに入っている |
| `data/upstream-differential/operations.json` | 記録本体（約2MB、コミット済み） |

再生成:

```bash
TM_SOURCE=C:/Users/takkun/AppData/Local/Temp/tm-src node scripts/build-upstream-differential.mjs
```

上流cloneは指定SHA・`npm install` 済み・テスト実行可能にしておくこと。

### この方式で踏んだ罠（同じ轍を踏まないために）

**(a) 事前状態は「呼び出しの前に、呼ばれた本人から」取る。** 2回踏んだ。

- *フレーム検索*: 「捕捉した全ゲームから同じIDのプレイヤーを探す」実装だと、
  テストが `beforeEach` の2人ゲームと本体の `testGame(1)` を両方作る場合に
  最初のゲームの手つかずのプレイヤーを拾う
- *事後読み*: 直そうとして直読みにしたが、記録を組む位置が `original.apply()` の
  **後**だった。一致していた76件が全部差分になった

差分は**増える方向にも減る方向にも転ぶ**ので、件数を見ても異常に気づけない。

**(b) テストが直接代入する値はメソッドラップでは見えない。**
`card.resourceCount = 5`、`player.canUseHeatAsMegaCredits = true`、
`player.tagsForTest`、`game.stJosephCathedrals` は個別に記録している。

**(c) 内部関数ではなく実経路を通す。ただしAPIごとに深さを合わせる。**

- `card.play` → `executeGameCommand(PLAY_CARD)`。`applyCardEffect` 直叩きだと
  「自分のタグを含めて数える」カード（Cartel、Satellites）が全部0になる
- `card.action` → `applyCardAction`。上流の `card.action()` は
  `Player.playActionCard` の**内側**（ログ・使用済み登録・手番消費の手前）

**(d) 「N件一致」の前に、その一致が意味を持つか確かめる。**
触られていないプレイヤーの行は「両方0だから一致」で無意味な一致を量産する。
**同じ記録内容で答えが true と false の両方ある行が203件**あり、うち22件が
照合対象に紛れていた（片方とたまたま一致していただけ）。いまは名前を付けて除外している。

### このハーネスで直したバグ

| # | カード / 箇所 | 内容 |
|---|---|---|
| 1 | CEO's Favorite Project | 資源の乗ったカード0枚でもプレイ可。MCを取り、カードが場に出ず消滅 |
| 2 | `spend.cards` | **アプリ全体で参照0件**。捨て札コストが一切課金されず |
| 3 | Astra Mechanica | 戻せるイベント0枚でも購入可 |
| 4 | Market Manipulation | Colonies無効でも購入可 |
| 5 | ドロー系34枚 | 山札が足りなくてもプレイ可（上流は `Deck.canDraw` で全カードに課す） |
| 6 | 研究フェーズ | 山札・捨て札が空だと**ゲーム完全停止** |
| 7 | ドラフト | 全員のキューが空だと**ゲーム完全停止**（4人戦で7,696件） |
| 8 | Law Suit | 常時-1点が0点。`specialVictoryKind` 4種のうち1種だけ表に未登録 |
| 9 | St. Joseph | 大聖堂を所有者で絞っていた。上流は誰が置いても全員が数える |
| 10 | Maxwell Base / Dirigibles | 置く先が無くてもアクション可能 |
| 11 | Hi-Tech Lab | 空デッキでもアクション可能 |
| 12 | Terraforming Contract | `requirements.tr` が**どこでも読まれず**、TR20で買えた |
| 13 | Energy Market | Helion の熱をMCとして数えず |
| 14 | Cyberia Systems | 建材カード2枚の要件が未実装 |
| 15 | ソロの攻撃カード | 上流は `isSoloMode()` で対象チェックを飛ばす |
| 16 | Tycho Magnetics | 空デッキでもアクション可能（**未コミット**） |

**#6・#7 は同じ構造**だった: 「終了判定が行動ハンドラの中にしかない」ため、
行動が0回だと永久に終わらない。状態を作った直後にも判定を置くこと。

**Kaguya Tech を実装**（緑地→都市、酸素不変、配置制限無視、配置ボーナス取得、
Hellas南極の配置費6MC込みで15MC不可/16MC可）。

### 直前セッションの3ファイルはコミット済み（`101f73e`）

`app/game-command.js`（#16 Tycho Magnetics）/
`scripts/audit-upstream-differential.mjs`（曖昧な記録203件の除外・486→464）/
`tests/strict-rules.test.mjs`（#16 の回帰テスト）。
push 済みで `origin/main` と同一。作業ツリーはクリーン。

**2026-09-02 の再着任時にベースラインを実測し直した**（引き継ぎの数字と一致）:

```
npm test                                    : 661 pass / 0 fail
node scripts/audit-upstream-differential.mjs: 464/464 一致・差分0・exit 0
npm run lint                                : 0 errors（warning 4件のみ）
npx tsc --noEmit | grep '^app/'             : 0件
上流clone tm-src                            : SHA 1b26fe69 のまま・node_modules 健在
```

### 中断: sol への相談（**2回とも失敗。回答は得られていない**）

`%TEMP%\mf-a15\prompt9.md`（「残りのスキップをどこまで開けるか」）を
**2回投げて2回とも落ちた**。`answer9.md` は存在しない。

| 試行 | 死に方 |
|---|---|
| 前セッション `b39w1inbn` | **1800秒でタイムアウト**（exit 124、codexプロセスをkill） |
| 再投入（2026-09-02） | **約220秒でクラッシュ**（exit -1、回答本文は空） |

2回目の stderr は上流clone `tm-src/src/server/Player.ts` の行番号
（25/170/608/1411/1415/1417/1839/1887）と `SerializedPlayer.ts:71` の羅列で、
**grepで上流を読んでいる最中に落ちた**形跡。`codex_status` は正常
（bridge v0.21.0 / codex-cli 0.152.1 / ログイン済み）なので**認証やインストールではない**。

**「時間が足りない」ではない**（220秒で落ちている）。疑わしいのは
**`workspace` の外（`%TEMP%	m-src`）を大量に読ませる構造**そのもの。
再挑戦するなら (a) 読むべき上流ファイルをパスで列挙して探索させない、
(b) 候補A/B/Cに3分割する、(c) 別バックエンド（cursor / antigravity）に振る。
なお **E2Eの設計を antigravity に投げたときは、範囲をワークスペース内に
閉じたら1回で通った**（§6.10）ので、(a)(c) には見込みがある。

**候補A・B・Cのどれをやるかは未決。** 以下は相談のために実測して渡した材料:

- **候補A: 場に他のカードがある 611件** — `playedProjects` への書き込みは
  **副作用なし**と実測。50件の試作で31件一致。動く見込みはある
- **候補B: 標準プロジェクト 190件** — **182件が `canAct`、動いた行は0件**。
  真偽値照合だけ。`true`/`false` が89/93とほぼ半々で材料としては良質。
  判定が `STANDARD_PROJECT` ハンドラの中にあるため pure な status 関数の抽出が要る。
  なお `Aquifer Pumping` / `Aquifer Turbines` / `City Parks` / `Trade Envoys` は
  **標準プロジェクトではない**。上流の指定SHAで実際にクラス宣言を確認した
  （2026-09-02）ので、これは推測ではない:

  | カード | 上流のパス | 宣言 |
  |---|---|---|
  | Aquifer Pumping | `src/server/cards/base/AquiferPumping.ts` | `extends ActionCard implements IActionCard, IProjectCard` |
  | Aquifer Turbines | `src/server/cards/prelude/AquiferTurbines.ts` | `extends PreludeCard` |
  | City Parks | `src/server/cards/promo/CityParks.ts` | `extends Card implements IProjectCard` |
  | Trade Envoys | `src/server/cards/colonies/TradeEnvoys.ts` | `extends Card implements IProjectCard` |

  つまり候補Bの212件のうち、この4つぶんの22件は
  **標準プロジェクトの再生では永久に開かない**。開けるのは残りだけ
- **候補C: タイル配置 181件** — 記録は所有者付き（`-` / `blue` / `red`）。
  ソロリグの `SPACE_IDS`・`TILE_TYPES` が流用できる。
  上流の席順とこちらの席順が一致する保証は**未確認**

### 開かないと決めたもの・開いていないもの

- **差分監査は資源・生産・TRしか比較しない。** Kaguya Tech のタイル種別を
  city→forest に変えても**監査は緑のまま通る**（回帰テストの方が捉える）。
  この限界はスクリプト冒頭にも書いてある
- **Turmoil 与党292件は開かない。** `PartyHooks.shouldApplyPolicy` が
  policyId・alliedParty・phase まで読むので、与党名だけでは再現できない。
  sol の判断で「追加しない」と決めた
- **企業・プレリュードの play 469件** — `SELECT_CORPORATION` と
  プレリュードセットアップの前後処理を含むため、`PLAY_CARD` には寄せられない
- **`initialAction` 17件** — 企業10枚分、未再生
- 上流が `play()` の外に遅延させた122件は、この方式では比較できない

### 誤報を1件出した（記録として）

Tycho Magnetics を「カタログ欠落」と報告したが誤り。`OFFICIAL_PROJECTS` だけを見て
結論していた。実際は**企業カード**として `CORPORATIONS` にある。
調べ直した結果、別の実バグ（#16）が見つかったので結果的に無駄ではなかったが、
**探す場所を1つしか見ずに「無い」と言わないこと**。

---

## 6.10 ブラウザE2E（Playwright、2026-09-02 導入）

`npm run test:e2e`。`test.yml` の Static build 直後に入っていて、
**落ちれば `deploy.yml` が走らないのでデプロイが止まる**。

### なぜ入れたか / 何を担当しないか

既存の検証（WSドライバ・`playtest.mjs`・監査23本）は**全部エンジンと話している**。
「エンジンが正しく計算し、DOMに出なかった」類はどれも捉えられない。

| 既存 | 担当 | E2Eが足すもの |
|---|---|---|
| WSドライバ | 全世代の同期・サーバー権威 | — 重複させない |
| `playtest.mjs` | 通しの進行・不変条件 | — 重複させない |
| 監査23本 | カードの数値適合 | — 重複させない |
| **Playwright** | — | **クリックを飲むモーダル・マウントしないボタン・DOMに出ない値** |

### 構成

| パス | 役割 |
|---|---|
| `playwright.config.ts` | `pages-static` プロジェクト。**1600x1200**（狭いと盤面下段が他パネルに潜ってクリックが落ちる） |
| `scripts/serve-static.mjs` | `static-dist/` を配信。**minify後の実物**を見るため |
| `tests/e2e/solo-production.spec.ts` | シナリオ1（行動パネル） |
| `tests/e2e/game-over.spec.ts` | シナリオ3（勝敗表示・4本） |
| `tests/e2e/fixtures/finished-games.mjs` | 終局セーブの生成（エンジン＋本物のシリアライザ） |

CI は `retries: 2`、ローカルは `retries: 0`（flakeを隠さない）。
**警告で通す設定はしない。1件でも赤にする。**

### シナリオ1が捉えるもの

ソロ開始→企業選択→初期購入→**都市の建設**→マス選択→行動パネル。
壊し方: `app/game-command.js` の `afterPlay` 内 `settled.lastAction = {...}` を消すと赤。

### シナリオ3が捉えるもの（勝敗表示・最優先）

**このプロジェクトが実際に事故った箇所**（冒頭参照: 勝っている側が負けと表示され、
テスト424件全緑・CI全緑のまま長期間残った）。直っていたが**回帰を検知する自動テストが無かった**。

終局には**裏口フックを足さない**。エンジンで終局状態を作り、**本物の
`serializeSavedState` で localStorage に置いて「継続」から入る**——
復帰プレイヤーと同じ経路。`tests/e2e/fixtures/finished-games.mjs` が
負け（68対20）・勝ち・同点・ソロの4つを作る。

**破壊確認済み**: 勝敗表示を旧実装（`gameResult === "win"` だけ見る）に戻すと
**マルチ3本が赤・ソロ1本は緑のまま**。ソロの文言は元々バグではないので、
この非対称が出るのが正しい。

- 落とし穴: **終局モーダルは初回マニュアルより上に出る**。
  マニュアルを閉じようとすると終局モーダルにクリックを奪われる。閉じる必要は無い

### 導入時に踏んだ罠（全部このE2Eが見つけた）

1. **初回は指令マニュアルが全面に出る。** `!gameState.onboarded` の
   `overlay-container` が企業選択パネルの上に乗ってクリックを飲む。
   実際の新規プレイヤーが見る画面なので、テスト側で閉じるのが正しい
   （`data-testid="onboarding-dismiss"`）
2. **発電所は固定費用ではない。** `cost: (state, corporation) => 11 - powerDiscount`
   で、Thorgate だと10。「MC -11」を検査すると**その企業を配られた回だけ落ちる**。
   小惑星14・海洋18・緑化23・都市25は `cost: () => N` の定数で安全
3. **企業の初期MCで買えないことがある。** TerraLabs 14 / PhoboLog 23 は
   都市25を買えない。`data-starting-mc` を出して**払える企業を選ぶ**ようにした
4. **行動パネルは1.3秒で消える。** 1項目ずつ `expect` すると
   その予算を食い潰して次が空振りする。**クリックの前に `waitFor` を張り、
   出た瞬間に `innerText()` で一度に取る**
5. **`data-testid` は minify を通る。** 既存の `data-mine="yes"` は
   `[data-mine=yes]` に変形するが、`data-testid` は素通しだった（実測）

### このE2Eが掘り出した実バグ2件

**(1) 確認ダイアログがドロワーに隠れていた（UI）**

標準プロジェクトはドロワーの中から押すのに、`confirmAction` は
**ドロワーを閉じていなかった**。`.choice-overlay` と `.drawer-scrim` は
**どちらも `z-index: 60`** で、後に描画されるドロワーが上に乗って
「実行する」を飲んでいた。特許売却だけは自前で `setOpenDrawer(null)` していたので
**8つのうち1つだけ動いていた**。`confirmAction` 側で閉じるように直した。

**(2) 場所を聞く標準プロジェクトが `lastAction` を残さない（エンジン）**

行動パネルは `lastAction` だけを見る。ところが `placeOrAsk` は
**合法マスが2つ以上あると `finishProject` を通らず** `pendingChoice` を返す。
その後始末をする `afterPlay` は**ログとしきい値ボーナスは復元するのに
`lastAction` を書いていなかった**。

結果、**都市・緑化・海洋は数字が動いても行動パネルが一切出なかった**。
合法マスが1つだけなら `finishProject` を通るので出る——
**盤面に余裕があるときだけ起きる**ので、テストもCIも全部緑のまま残っていた。
`tests/game-command.test.mjs` に回帰テストを追加（修正を戻すと赤になることを確認済み）。

> **§6.6 の「代表的な入力を選ぶ」の再演。** 小惑星（マスを聞かない）だけで
> 試していれば永久に見つからない。**そのゲームで最も普通の一手**は
> タイルを置く手である、という選び方がこの2件を出した。

### さらに掘り出した実バグ2件（2026-09-03）

**どちらも #2 を書く過程で出た。同じ「エンジンは正しく、DOMに出ない」型。**

**(3) 場所を聞く標準プロジェクトが、コストと生産量を報告しなかった**

都市を建てたときの行動パネルは `建材+2`（配置ボーナス）**だけ**を表示し、
**25MC払ったことも、MC生産が上がったことも一切出ていなかった。**

原因は差分の基準点。場所を聞くプロジェクトは**支払いも生産増加もコマンド時点で
起きる**（choiceが開いている最中）のに、`lastAction` は choice 解決まで書かれない。
`tableRef.current` はその間のレンダーで**無条件に前進していた**ので、
コストと生産を飲み込み、答えたあとに動いた配置ボーナスだけが残っていた。

```
BEFORE      mc=100 mcProd=0
AFTER CMD   mc=75  mcProd=1  pendingChoice=tile-placement   ← ここで動く
AFTER PICK  mc=75  mcProd=1                                  ← ボーナスだけが後
```

修正は `page.tsx` で **choice が開いている間は基準点を進めない**。

> **既存のアサーションはこれを通していた。** 「`changes` が空でない」しか
> 見ておらず、`建材+2` は空ではないから。**「空でない」は検証ではない。**

**(4) 定数でないものを定数として検査していた（3回目）**

`before + 1` と `MC−25` を決め打ちしていたが、**Tharsis Republic は都市ごとに
MC生産+1、自分の都市に3MC返す**ので、その企業が配られた回だけ落ちる。

| 回 | 定数のつもりだったもの | 実際 |
|---|---|---|
| 1 | 発電所11MC | `11 - powerDiscount`（Thorgate は10） |
| 2 | 都市の生産+1 | Tharsis Republic だと +2 |
| 3 | 都市の価格 25MC | Tharsis Republic だと実質 −22 |

**企業は「定数」を壊す最大の要因**。数値を決め打ちする前に、
その値に触る企業効果が無いか `official-content.js` を見ること。
いまは**パネルが報告した増分をマットが一致させる**形にしてある。

#### 行動パネルは1300msで消える（`waitFor` では取り逃す）

`waitFor` はポーリングなので、**負荷がかかるとサンプル2回の隙間に窓が
丸ごと通過する**。並列ワーカー下で実際に起きた。CIの `retries: 2` は
これを直さず隠すだけ。

**クリックの前に MutationObserver を張って、マウントした瞬間のテキストを
`window` に記録する**（`solo-production.spec.ts` / `tile-placement.spec.ts`）。
そのとき**「最初の非空」ではなく「最も長い」を採る**——Reactは delta を
数回のミューテーションに分けて追加するので、最初のサンプルは半分しか無い。
カットインのように**同種の要素が複数出るもの**は、長さではなく**名前で選ぶ**
（`気温`/`酸素`/`海洋` は全部2文字なので、長さ比較では先着に化ける）。

---

### シナリオ一覧（5本すべて実装済み、2026-09-03）

| # | 名前 | project | 壊して赤にした箇所 |
|---|---|---|---|
| 1 | 行動パネル | static | `game-command.js` の `afterPlay` 内 `settled.lastAction` |
| 2 | タイル配置と盤面 | static | 盤面クリックの解決（`page.tsx:1980`）／カットイン発火（`page.tsx:663`） |
| 3 | 勝敗表示（4本） | static | 勝敗表示を旧実装（`gameResult === "win"` だけ見る）に戻す |
| 4 | ソロ版ビルドのゲート（2本） | static | `title-screen.tsx` の `disabled`／`page.tsx:1726` の `ONLINE_ENABLED` |
| 5 | オンライン2画面 | **online** | `net-protocol.js` の `hydrateView()` を外す |

**全部「赤にできること」を確認してから採用している。** 緑で通ったから入れた、
というものは1本も無い。

#### #4 が守っているもの

`NEXT_PUBLIC_SOLO_ONLY=1` は**3箇所**を塞いでいて、うち2箇所がDOMから見える
（タイトルの `mode-online`、ヘッダのロビーボタン）。ユニットテストはモジュールを
importするだけ、監査はDOMを見ない。**ゲートが1つ落ちても全部緑のまま、
ロビーだけが繋がらない状態で配信され得た。**

#### #5 は別プロジェクト

`playwright.config.ts` は2プロジェクト構成になっている。

| project | 対象 | サーバー | CI |
|---|---|---|---|
| `pages-static` | 8本 | `serve-static.mjs` | **ブロッキング**（デプロイを止める） |
| `online` | 1本 | `npm run dev`（wrangler + DO） | `continue-on-error` |

`testIgnore: /online-/` と `testMatch: /online-/` で振り分けている。
ファイル名を `online-` で始めれば online 側に入る。

**online が非ブロッキングなのは、hosted runner での実績がまだ1回だからで、
落ちてよいという意味ではない。** 2026-09-03 に ubuntu-latest で実際に通ることは
確認済み（15.4秒）。**`continue-on-error` はステップが緑でも中で落ちていることが
あるので、昇格を判断するときはログの `1 passed` を読むこと。**

#### #5 を書くときに踏んだもの

- **dev サーバーはハイドレーションが遅い。** `mode-online` が enabled かつ visible
  なのにクリックしてもハンドラが走らない時間帯がある。**「ボタンが死んでいる」
  のと完全に同じ見え方をする**ので、製品バグと誤診しやすい。
  `goto("/", { waitUntil: "networkidle" })` で解消する
- **`finally` の `context.close()` が本当のエラーを隠す。** 最初の赤は
  "Target page has been closed" としか言わなかった。`.catch(() => {})` を付ける
- **既定のタイムアウト15秒では2ブラウザ分の握手に足りない。** spec が何を見つけたか
  言う前に打ち切られる。online プロジェクトは 90秒（CI 120秒）にしてある
- **部屋コードの表示だけ掴めなかった**ので `data-testid="room-code"` を足した。
  他の操作要素は role と placeholder で届く

### 外部レビューの精度（記録として）

E2Eの設計は外部AI（agy）に出させたが、**そのまま実装できる状態ではなかった**。
行番号の指摘は概ね正確だった一方で、以下3つは実測で覆した:

1. **標準プロジェクトのボタン行が全部ズレていた。** 指定されたのは
   ラベルの `<div>` で、ボタンは4〜9行下。**都市と特許売却が丸ごと抜けていた**
2. **「発電所11MCは固定費用」が誤り。**
   `cost: (state, corporation) => 11 - powerDiscount` で Thorgate だと10。
   flake対策の柱に据えられていたが、**それ自体がflakeの原因**だった。
   固定なのは小惑星14・海洋18・緑化23・都市25（`cost: () => N`）
3. **`window.__E2E_TRIGGER_GAME_OVER__` は存在しない。**
   工数見積もりにその実装分も入っていなかった。
   そもそも**テスト専用の裏口を本番バンドルに足すのは
   「実経路を通す」規律に反する**ので、セーブ流し込みに替えた

**外部の設計は素材であって指示ではない。** 行番号・定数・フックの実在は
**全部こちらで確かめてから**書くこと。

## 6.11 オンライン通しドライバ（`scripts/online-playtest.mjs`、2026-09-03）

**新しいドライバを書くときは `scripts/online-harness.mjs` の `createOnlineRun()` を使う。**
部屋コードとWebSocket URLの唯一の入口で、`tests/online-harness.test.mjs` が強制する（§7.1.1）。

2席ぶんのWebSocketクライアントで**全世代を約1.5秒**で走らせる。
UIと同じ経路（サーバーが権威）なので実機確認として通用する。

```bash
npm run dev &                        # wrangler + Durable Objects
node scripts/online-playtest.mjs     # 部屋コードは自動生成
node scripts/online-playtest.mjs ABCDE   # 明示もできる（5文字・正規化を通ること）
```

**実測（8戦、2026-09-03）**: 全戦終局・世代11〜12・**勝者は常に高得点側**・
サーバーエラー0・異常0。企業もスコアも毎回異なる（＝試行が独立している）。

### 何を見ているか

- 各世代のグローバルパラメータが**下がっていないか**（上がる一方が正しい）
- 世代が**逆行していないか**
- サーバーからの `{"type":"error"}` を**全部、送った内容つきで**記録
- 終局時に `winnerPlayerIds` が**standings の最高得点と一致するか**

### 書くときの前提（引き継ぎ書に無くて2回踏んだ）

- **view の `currentPlayerId` は閲覧者に書き換えられている**（`net-protocol.js:156`）。
  本当の手番は **`turnHolderId`**。前者で手番判定すると**全員が自分の手番に見える**
- **`setupStep` はトップレベルではなくプレイヤーごと**。企業候補は
  `me.corporationOptions`（ID文字列の配列。`corporationChoices` ではない）
- 部屋コードは**5文字に正規化される**（§2.9）。ドライバはガードを持っている

§6.8 に載っている骨組みと合わせて読むこと。

### E2E（#5）との分担

| | 担当 |
|---|---|
| このドライバ | 全世代の進行・サーバー権威・勝敗の整合 |
| E2E #5 | **実ブラウザ2枚が何を描画するか**・途中で例外を投げないか |

**重複させないこと。** #5 は企業選択画面で意図的に止めている。

---

## 6.12 切断復帰の検査（`scripts/online-reconnect-check.mjs`、2026-09-03）

§6.1 で長く「❌ 未確認」だった切断復帰を、実際に落として確かめる。

```bash
npm run dev &
node scripts/online-reconnect-check.mjs   # 成功=exit 0 / 失敗=exit 1
```

### 設計（solの助言に沿っている）

**手番を持っている席を落とす。** 相手が勝手に進んでごまかせないので、
席の喪失・状態の再送漏れ・復帰後の拒否が**それぞれ別のアサートで落ちる**。

- `close()` ではなく **`ws.terminate()`**。正常終了は「さよなら」であって、
  実際のネットワークが起こす**不作法な切断**とは別物
- 同じ `playerId` で再接続する（＝新規参加ではなく復帰）
- 落とす前に席ID・世代・フェーズ・`turnHolderId`・`lastAction.seq`・MC・手札枚数を控え、
  復帰後に**全部一致すること**を見る
- **読めるだけでは不合格。** 復帰後に実際に行動を送り、`lastAction.seq` が進み、
  その行動が**元の席に記録される**ことまで見る（黙って読み取り専用になる席を捕まえる）

### 赤にして確認した（2件）

| 壊した箇所 | 結果 |
|---|---|
| `onJoin` の `existing` を常に undefined（＝復帰を忘れる） | 赤 |
| 復帰時に `seatMap` から席を削除 | 赤 |

### 限界（誤解しないこと）

**これはサーバーとの復帰契約の検査であって、`use-room.ts` のバックオフを
実行するものではない。** クライアント側の再試行間隔・上限は別途未検証。
**通信量も測っていない**（§6.1 の「通信量」は依然として未確認）。

---

---

## 7. 開発の進め方

### コマンド

```bash
npm test                 # ビルド + 662テスト
npm run test:e2e         # Playwright 9本（static 8 + online 1）
npm run lint             # ESLint
npx tsc --noEmit -p tsconfig.json | grep '^app/'   # 型（worker/ の既存エラーは無視）
npx playwright test --project=pages-static   # CIと同じ呼び方（ブロッキング側）
npx playwright test --project=online         # 同上（wrangler を立てる側）
node scripts/playtest.mjs --games=20 --players=2 --colonies --turmoil
node scripts/online-playtest.mjs   # オンライン1戦を通しで（要 npm run dev）
npm run dev              # localhost:3000
npm run deploy           # Workers へ手動デプロイ（緊急時のみ。通常は不要）
git push origin main     # 2つの公開先が両方とも自動で更新される
```

### 7.1 測定そのものを疑う（このセッションで2回間違えた）

このプロジェクトの規律は「テストではなく実測」だが、**実測にも嘘のつき方がある**。
このセッションは「実測した」と言える形で2回間違えた。どちらも報告まで到達した。

| 何を報告したか | 実際は | 見抜けた合図 |
|---|---|---|
| 「オンラインは毎回同じ山札」 | 8戦とも**同じ1部屋**（§2.9） | 結果が**同一すぎた** |
| 「修正後も直っていない」 | **死んだサーバー**を叩いていた | ポート番号を読んでいなかった |

再発防止として、次を作業手順に入れる。

1. **試行が独立していることを、試行の中で確かめる。** 「毎回ユニークなつもり」は
   根拠ではない。IDを生成したら、**サーバーが受け取った後のIDを読み返して**
   食い違いを検出する。`online-playtest.mjs` のコードガードがこの形
2. **同一すぎる結果は成功ではなく異常。** 独立試行が全部バイト単位で一致したら、
   まず**試行が独立していない可能性**を疑う。乱数の欠陥だと結論するのは最後
3. **「修正後」を測る前に、測る相手が修正後のものか確かめる。**
   サーバーを再起動したら**ポート番号を読む**（`Port 3000 is in use, trying another one`
   が出ていたら別ポートにいる）。プロセスを止めたら**止まったことを確認する**
   （このセッションでは TaskStop がプロセスを殺せておらず、2つのサーバーが同時に生きていた）
4. **修正が効いていることを、修正を外して確かめる。** 3で見た緑が本物なら、
   修正を戻すと赤になる。ならなければ**その修正は何もしていない**
   （seed修正はこれで「不要だった」と判明した）
5. **報告の前に「これは何を測ったのか」を一文で言えるか確認する。**
   「8戦回して全部同じだった」は測定結果だが、「8つの独立した部屋で」が
   言えないなら、それは主張の半分が未確認

> **推測を報告するのと同じくらい、間違ったものを測って報告するのは質が悪い。**
> 後者は「実測した」という形をしているぶん、読む側が疑えない。

外部AI（sol / Gemini）に相談する場合も同じで、**相談の前提そのものが
間違っていることがある**。このセッションでは存在しないバグについて sol に
設計相談を投げてしまった（回答自体は妥当だったが、直す対象が無かった）。
**相談の前に、その現象を最小の再現手順で1回は自分で確かめること。**


### 7.1.1 規範は文章ではなく機構にする

**上の5項目は文章であって、文章は読み飛ばされる。** 実際、部屋コードの罠は
§2.9 に書いた後でも、新しいドライバを書けば同じように踏める形のままだった
（ガードが2ファイルにコピペされていただけで、3本目には存在しないから）。

**いまは機構になっている**（2026-09-03）:

| 防ぐもの | 機構 |
|---|---|
| 部屋コードの衝突 | `scripts/online-harness.mjs` の `createOnlineRun()` が唯一の入口 |
| ガードの付け忘れ | `tests/online-harness.test.mjs` が **`new WebSocket` を使う `scripts/*.mjs` を自動検出**して強制 |
| URLの手組み | 同テストが `/api/room/` の直書きを禁止 |
| 同一プロセスでのコード再利用 | ハーネスが発行済みコードを拒否 |

**ファイル名に依存せず検出する**ので、`online-` で始まらない名前で
新しいドライバを書いても捕まる。**赤にして確認済み**:

| 壊し方 | メッセージ |
|---|---|
| ハーネスを使わない3本目を追加 | `rogue-driver.mjs must import createOnlineRun...` |
| コード検証を無効化 | `Missing expected exception.`（`TMTL7V2EW` が通ってしまう） |
| ドライバでURLを手組み | `must build room WebSocket URLs through the online harness` |

ハーネスは**本体の `generateRoomCode` / `isValidRoomCode` を再利用**している。
アルファベットを2箇所に持たないため、本体が変わってもドライバ側が古い定義で
動き続けることがない。

> **迂回まで防ぐ境界ではない**（`WebSocket` を別名に置き換えれば検出を外れる）。
> 悪意ではなく**うっかり**を止めるための機構、という位置づけ。

### 7.1.2 サーバーの取り違え（mistake 2）は未実装

**「ポートに何かがいるか」の確認では不十分**、というのが sol の判断で、これは正しい。
古いサーバーが3000番にいる場合、それを「正常」と判定してしまい、**今回の事故を
まさに素通りさせる**。誤った安心を足すだけになる。

正しくやるなら**起動と測定を1つのランナーが所有する**:
ポートの未使用を確認 → ランナー自身がサーバーを起動 → 想定外ポートへの
フォールバックを検出したら失敗 → 確定したURLだけをドライバへ渡す →
自分が起動したプロセスだけを止める。

さらに強くするなら、起動ごとのランダムIDやbuild fingerprintをサーバーが返し、
ドライバが照合する。ただし**dev専用エンドポイントが要る**ので、
「ユーザー向けに何も足さない」方針とぶつかる。**未実装。**

当面の運用: **サーバーを再起動したらポート番号を読む**
（`Port 3000 is in use, trying another one` が出ていたら別ポートにいる）。
プロセスを止めたら**止まったことを確認する**。

### 7.1.3 外部AIに書かせている間、同じファイルを触らない

**このセッションで実際にやった。** sol に書き込み権限で同じ課題を投げながら、
待っている間に自分でも同じファイルを書いて**互いに上書きした**。
さらに復旧時の `git checkout` で **sol の変更を1ファイル分消した**
（コミット済みの版に戻るため）。

- **投げたら、そのファイル群には触らない。** 別の作業をするか、待つ
- **復旧に `git checkout` を使わない。** 未コミットの他人の作業が消える。
  退避コピーから戻すこと
- 終わったら `git status` で**何が変わったかを先に見る**

### 作業の型

このセッションで有効だった手順:

0. **最後まで実際に遊ぶ。** テストは「書かれた処理が正しいか」しか見ない。
   **書かれていない処理**（マルチの勝者判定）は、424件全緑・CI全緑・
   プレイテスト0件のまま見逃されていた。終局画面まで到達して
   はじめて「勝っている側が負けと表示される」と分かる

0-b. **「遊んだ」の中身を疑う。** 何を試したかで結果が変わる。
   - **代表的な入力を選ぶ。** 行動パネルを小惑星でだけ試して合格にし、
     **生産カードを一度も打たなかった**ため、生産量が表示されない欠陥を
     本番まで出した（§6.6）。そのゲームで最も普通の一手は何かを先に考える
   - **手でクリックできる範囲は狭い。** オンラインを2タブで手動確認したときは
     企業選択までしか見ておらず、**世代2のデッドロックを見逃した**（§6.8）
   - **長い経路はドライバを書く。** `ws` で WebSocket クライアントを2席ぶん
     動かせば、全世代を数十秒で通せる。UI と同じ経路（サーバーが権威）なので
     実機確認として通用する。書き方は §6.8 に記録した。
     **毎回きっかり同じ世代で止まる**ような不具合は、これでしか見つからない
1. **指摘を鵜呑みにせず、現HEADで実測する。**
   一時スクリプトで状態の前後差分を取る。コード読解では
   「`ok=false` なのに state が汚れている」種類のバグを見落とす
2. **修正前に、その挙動を再現するテストを書く。**
   修正後に通ることと、**修正前に落ちること**の両方を確認する
   （`git stash` で戻して確認できる）
3. **フルスイートを3回連続**（§2.6）
4. プレイテストを全拡張構成で回す

外部監査は3回受けた。指摘の精度は高かったが、
**現HEADでは既に直っていたもの、文言が不正確なもの、
そのまま従うと新しいバグを作るものが混ざっていた。**
1件は「ボットも直接呼んでいる」という指摘で、従うと二重消費になるところだった。
一方で「攻撃カードの修正が Law Suit の前提」という指摘は、
**単独で最も重いバグ**（自分を攻撃する10枚）を掘り当てた。

**数の決め打ちも疑う。** 「6枚」と指定された攻撃カードは、
機械的に抽出すると7枚で、指定に含まれていた1枚は攻撃カードですらなかった。

### 外部AIの利用

- **Codex**（`mcp__codex__codex_ask`）: カードアート428枚のSVG生成、
  カード名381枚の翻訳、PDF解析。ファイル書き込みができる
- **sol**（`ask_codex.ps1 -Model gpt-5.6-sol -Effort high`）: 設計相談・実装。
  **`-Effort` が正しいフラグ名**（`-ReasoningEffort` は存在しない）。
  **既定は read-only。書かせるなら明示的に指定する**:

```powershell
pwsh -NoProfile -File ~/.claude/orchestration/bin/ask_codex.ps1 `
  -PromptFile <in.md> -OutFile <out.md> -Model gpt-5.6-sol -Effort high `
  -TimeoutSec 1500 -Sandbox workspace-write -WorkDir "C:/Users/takkun/Documents/mars-frontier"
```

  2026-09-03 に書き込み権限で回して、ホットシート導線の実装まで通した（§6.4）。
  **ワークスペース内に閉じた質問なら安定する。**
  過去に2回落ちたのは、いずれも `%TEMP%	m-src`（上流clone）を
  大量に読ませる構造だったとき（§6.5）。

- **Gemini / agy**（`agy --model "Gemini 3.8 Flash (High)"`）:
  E2Eの設計・下書き。**2026-09-03 に個人クォータ枯渇（リセット144時間）。**
  - **MCPブリッジ（`mcp__agy__antigravity_ask`）はモデル指定が壊れている。**
    表示ラベルもスラッグも拒否し、エラーには `slug<TAB>ラベル` の連結を
    「有効な値」として並べる。CLIは**表示ラベルしか受け付けない**ので
    両者が食い違っている（agy CLI v1.1.25 が bridge の検証より新しいため）。
    当面は `agy --model "Gemini 3.8 Flash (High)"` を直接呼ぶ
  - **agy には独自の `--print-timeout`（既定5分）がある。** 長い作業では
    `--print-timeout 25m` を付ける。付けないと `timeout waiting for response`
  - **`--dangerously-skip-permissions` は Claude Code の分類器にブロックされる**
  - **ブラウザツールは持っていない**（保有ツールを列挙させて確認）。
    `read_url_content` はHTMLを取るだけで、SPAの状態は見えない。
    ブラウザを使わせたいなら `run_command` 経由で Playwright を書かせる

> **外部の成果物は素材であって指示ではない**（§6.10 の記録）。
> sol がホットシートに付けたテストも、**こちらで赤にして確認してから**採用した。
> sol 自身が「破壊確認はしていない」と申告した項目もあった。**申告を読むこと。**

---

## 8. 参考資料の場所

> **2026-08-17 時点で、下の2つはどちらも消えている。**
> `Downloads\MARS拡張ルール\` ディレクトリは存在せず、全文書き起こしも無い。
> `Downloads\TM_HE_WRAP_ENGi.pdf` だけが残っている。
> **リファレンス実装 `%TEMP%\tm-reference` も空のディレクトリだけで、`.ts` は0件**
> （生成物はコミット済みなので再生成しない限り困らない）。
>
> つまり**いま手元に一次資料は無い**。ルールの根拠が要る変更をするときは、
> 先に公式PDFを取り直すこと（§8末尾のURL）。過去の監査記録を
> 「出典を確認した」の代わりに使わない。

```
C:\Users\takkun\Downloads\MARS拡張ルール\     公式ルールブックPDF 10冊  ← 消失
C:\Users\takkun\Downloads\TM_RULEBOOK_JPN-reprint2018w_全文書き起こし.txt  ← 消失
```

**PDFからのテキスト抽出には注意が要る。** フォントが数字を独自コードに
割り当てているため、素朴に抽出すると `5 cards` が `ffl cards` に化ける。
ToUnicode CMap を解析する必要がある（`scratchpad/pdfx.py` に実装済み）。

盤面データは**画像認識ではなくリファレンス実装から移植した**。
`github.com/terraforming-mars/terraforming-mars` の `src/server/boards/*Board.ts` を
curl で取得できる。既存のタルシス盤面と1行ずつ一致することを確認済みで、
この経路は信頼できる。

---

## 付録0: `b2cd8d6`「complete audited robot opponent mode」（2026-08-14）

**このコミットは本文が空**なので、差分から読み取った内容をここに残す。
12ファイル・+1176/−669行。テストは401→418件。

| 領域 | 変更 |
|---|---|
| `game-command.js` | 最終緑化を2コマンド化。`getLegalCommands` が称号・褒賞・標準プロジェクト・植民地・代表者まで列挙し、**実行してみて状態が変わったものだけ**を返す。`legalCardPayment` で鋼材／チタン払いを総当たり |
| `game-logic.js` | `placeTileAt` に `finalGreenery`。気温0°Cボーナスを `pendingChoice` へ一本化。`openOrEnqueuePendingChoice` を追加 |
| `player-state.js` | `cloneGameState` の取りこぼし修正（`playedEvents` / `claimedMilestones` / `fundedAwards` / `scoreModifiers` / `boardMarkers` / `generationAttackLedger` / `resolvedChoices` / `turmoil` / `colonies` / `pendingChoiceQueue`）。`clonePendingChoice` を再帰コピーへ |
| `page.tsx` | −259行。最終緑化の自前実装とTR巻き戻しを削除 |
| `bot-player.js` | 866行の書き換え。合法手を自前生成せずエンジンに訊く形へ |

> **`cloneGameState` の取りこぼしは §2.1 の一族。** 浅いコピーで
> `pendingChoiceQueue` や `turmoil` が共有されると、
> **片方の分岐が他方の盤面を書き換える**。例外は出ない。

**未確認**: このコミットはブラウザ実機で確認されていない。
特に最終緑化のコマンド化（`page.tsx` −259行）は経路が丸ごと変わっている。

---

## 付録: 2026-08-07 の再監査で直したもの

公式ルールブックと突き合わせて `CONFIRMED BUG` と判定したものだけを直した。

| コミット | 内容 |
|---|---|
| `1567100` | Preludeソロ12世代 / 植民地トラック上昇 / Party Leader同数 / 有料代表者5MC |
| `3adfef4` | **旧与党のボーナスが支払われていた** / 与党の代表者が盤上に残っていた |
| `4f353aa` | World Government Terraforming（Venus、Solar phase step 2）を実装 |
| `e8e519c` | 世界的イベントの初期配置（current に3枚目を配っていた）と中立代表者の配置 |
| `2161484` | 世界的イベント36枚の効果を実装（それまではログ1行だけ） |
| `9c459de` | Turmoil最終得点（党首・議長）／得点内訳のカテゴリがNaNになる不具合 |
| `663a162` | 拡張別のイベント絞り込み（31枚 vs 36枚） |
| `a6f9bcd` | 選択の要らないイベント4枚（Election / Revolution / Cloud Societies / Sponsored Projects） |
| `66082c4` | WGT を第1プレイヤーの選択に。Solar Phase を中断可能へ |
| `2c924f8` | 残り4枚（Aquifer / Dry Deserts / Paradigm Breakdown / Corrosive Rain） |
| `c469bfe` | Generous Funding / Red Influence のoff-by-one、Diversityの影響力計算とタグ集計 |
| `e33083d` | **選択キュー**。複数プレイヤーへの順次質問。キューの秘匿漏れも修正 |
| `e776e25` | パラメータ変更の共通経路。WGT/イベントでも閾値が発火 |
| `a78cc15` | Arctic Algae / Aphrodite を盤面監視の効果として実装 |
| `354a47a` | 宣言だけで未処理のspecキーをテストで落とす |
| `df24cd8` | 選択の検証がクライアント入力ではなくエンジン側であることを固定 |
| `be07659` | **ホットシートの席が質問の持ち主に移らない**（ブラウザで発見） |

**監査で却下したもの**: `computeDominantParty` の同数処理は
公式の "or clockwise in case of tie" と一致しており `ALREADY CORRECT`。
Party Leader の同数処理は指摘自体は正しかったが、
**「配列の先頭が選ばれる」という説明は不正確**で、
配列順と現職が一致する並びでは再現しない（§2.2）。

---

## 付録2: それ以前のセッションで直したもの

参照用。**詳細は各コミットメッセージにある。**

| コミット | 内容 |
|---|---|
| `c054678`〜`3e79b84` | アクション実行経路の一本化（全8フェーズ） |
| `e0167c7` | 到達不能な配置分岐を削除（164行減） |
| `3e83f27` | Prelude の得点が入らない／交易テストのフレーキー |
| `f743c2f` | `all: true` の2枚（Immigration Shuttles / Space Port Colony）が0点 |
| `9a79be1` | 動的VPカード38枚中17枚がカード面に `?` と表示 |
| `b6fc34a` | 得点計算を寄与ベースへ。負の得点・他人への加点が書ける形に |
| `cf376d5` | **攻撃カードが自分を攻撃していた**（10枚）。生産量攻撃の消失 |
| `a3822c0` | イベントのタグが永続／Preludeのタグが数えられない |
| `614ab38` | Vitor が動的VPに反応しない／Botがアクションを消費しない／内訳と合計の不一致 |
| `4df3866` | 特殊VP3枚の効果、攻撃カード7枚のモデル化 |
| `dad7643` | 特殊VP3枚のカード面表示 |
| （最新） | Virus の動物2個分岐／Special Permit の実プレイ経路テスト |
