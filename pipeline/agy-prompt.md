Read `pipeline/spec.md` and `pipeline/rubric.md`, then implement the complete requested game in this workspace.

You own the implementation phase. Stay within the specification and do not make unrelated changes. Preserve the existing vinext/Sites architecture. Use the RED CONTROL design brief from the spec. The application must be a real, playable single-player game, not a static mockup.

After implementation:

1. Run the relevant build and tests.
2. Fix failures.
3. Write a concise list of changed files, gameplay implemented, and exact verification results to `pipeline/result.md`.

Do not rely on stdout as the deliverable; `pipeline/result.md` is required.
