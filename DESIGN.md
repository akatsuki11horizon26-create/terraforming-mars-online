# MARS FRONTIER UI規範

このゲームのUIが従う規則。見た目の好みではなく、**測定できる根拠**を持たせるために書いている。

## 直した問題

改修前のUIには3つの実測可能な欠陥があった。

**1. 本文色がWCAG AAを満たしていなかった**

`rust (#A83220)` はパネル面 `#121110` に対してコントラスト比 **2.82:1**。AA基準は4.5:1なので不足していた。この色を枠線と文字の両方に使っていたため、画面全体が「赤くて見にくい」状態になっていた。

**2. 背景の層に段差がなかった**

地の色 `#080908` とパネル `#121110` の比が **1.06:1**。ほぼ同一で、パネルが浮いて見えなかった。

**3. 角丸が6種類混在していた**

2px / 3px / 4px / 6px / 8px / 50% が無秩序に使われていた。角丸を多用した暗色UIは、生成物にありがちな没個性な見た目になる。

## 配色

### 面（背景）

暗色テーマでは純黒を使わない。明るい要素との差が強すぎて眼精疲労を起こすため。青みを持たせた濃紺系を基準にし、**上に重なる面ほど明るく**する。

| トークン | 値 | 用途 | 直下の面との比 |
|---|---|---|---|
| `--surface-base` | `#080C12` | ページ地 | — |
| `--surface-1` | `#141C28` | パネル面 | 1.14 |
| `--surface-2` | `#222E3E` | パネル内の区画・入力欄 | 1.25 |
| `--surface-3` | `#334155` | 押下可能な要素・強調 | 1.33 |

地から最上位まで **1.89:1**。層の順序だけで奥行きが分かる。

### 文字

純白は使わない。わずかに青を含ませた白の方が暗色面で目に優しい。

| トークン | 値 | 対 `surface-1` | 用途 |
|---|---|---|---|
| `--text-hi` | `#E8EDF4` | 14.86 | 見出し・数値 |
| `--text-mid` | `#A9B6C6` | 8.49 | 本文 |
| `--text-low` | `#7C8899` | 4.86 | 補足 |

`--text-low` は `--surface-3` 上では 2.88 で不足するため、**明るい面では使わない**。

### アクセント

火星＝赤という連想から赤系を全面に使うと、可読性が落ちるうえ警告色と区別がつかなくなる。**赤系はアクセントに限定**し、面と文字は中立色にする。

| トークン | 値 | 対 `surface-1` | 意味 |
|---|---|---|---|
| `--accent-amber` | `#F0A23C` | 8.28 | 主操作・MC・資源 |
| `--accent-ember` | `#FF7A4D` | 6.78 | 火星・熱・気温 |
| `--accent-cyan` | `#5FD3E0` | 9.89 | 海洋・情報・オンライン |
| `--accent-green` | `#68D391` | 9.42 | 植物・緑地・成功 |
| `--accent-violet` | `#A78BDB` | 6.13 | 宇宙・ジョビアン |
| `--accent-rose` | `#F2708C` | 6.22 | 警告・不可 |

全色が `surface-1` に対してAA（4.5:1）を満たす。

## 形状

**角丸を使わない。** 代わりに**面取り（chamfer）**で角を落とす。`clip-path` の多角形で実装する。SF系ゲームUIが角丸を避け、切り落とした角や斜めの縁を使うのは、機械的・計器的な印象を作るため。

```css
/* 2px面取り: 小さな要素 */
clip-path: polygon(2px 0, 100% 0, 100% calc(100% - 2px), calc(100% - 2px) 100%, 0 100%, 0 2px);
```

対角の2箇所だけを落とすと方向性が出て、全周を落とすより締まって見える。

例外は円形要素（代表者のドットなど）のみ。

## 1画面に収める

盤面・手札・ログ・パネルを**スクロールなしで**同時に見せる。カードゲームは全体の状況を把握しながら判断するため、スクロールで情報が隠れると判断が鈍る。

- `100dvh` 固定（`vh` ではない。モバイルのURLバーで高さが変わるため）
- 3カラム: 左=情報 / 中央=盤面 / 右=操作
- 各カラム内部でのみスクロールを許す
- 手札は中央下部に横スクロールで並べる
- 縦が足りない環境（900px未満）では盤面を縮小し、カラム構成は保つ

## 参照

- [Designing a Scalable and Accessible Dark Theme](https://www.fourzerothree.in/p/scalable-accessible-dark-mode) — 純黒を避ける根拠、面の重ね方、暗色面での彩度の扱い
- [Creating an Accessible Color Palette (Harvard)](https://accessibility.huit.harvard.edu/color) — コントラスト比の基準
- [InclusiveColors](https://www.inclusivecolors.com/) — WCAG準拠パレットの検証
- WCAG 2.1 AA: 通常文字 4.5:1 / 大きい文字 3:1

## 検証

配色は `npm test` の `tests/design-tokens.test.mjs` が検証する。トークンを変えてコントラストが基準を割ると失敗する。目視ではなく計算で担保する。
